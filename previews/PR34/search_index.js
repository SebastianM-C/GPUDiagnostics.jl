var documenterSearchIndex = {"docs":
[{"category":"section","location":"resources_mix/#Resource-report-and-instruction-mix","page":"Resource report and instruction mix","text":"cks = compiled_kernels(backend; pattern = r\"_my_driver!\")   # kernels this process compiled\nr = kernel_resources(backend, only(cks))                    # at the kernel's static workgroup size\nr.registers, r.local_mem_bytes, r.shared_mem_bytes           # per-thread regs, spill/stack bytes, LDS/block (missing if unreported)\nr.occupancy.active_blocks_per_sm, r.occupancy.fraction       # the runtime's occupancy calculator (missing without one)\nr.isa                                                        # AMD: sgpr/vgpr/spill counts, compiler occupancy\nr                                                            # prints as a small table\n\nBoth vendor packages cache every kernel instance the process compiles, so the inventory reaches kernels that are closures inside driver functions (an AcceleratedKernels foreachindex body, say) without wrapping, recompiling or modifying them; on Julia ≥ 1.12 the closure type carries the enclosing function's name, which is what pattern matches. shared_mem_bytes is what the kernel descriptor reserves — LLVM's AMDGPU backend promotes private arrays it cannot keep in registers to LDS, sized for the kernel's maximum block size, and that reservation, not the source, is what caps the resident blocks per CU. On NVIDIA the report also re-runs CUDA.jl's bundled ptxas --verbose on the regenerated module PTX, which separates the call-ABI stack frame from true register spills and lists the device functions assembled out of line (what CUDABackend(always_inline = true) removes). Nothing is launched; the ISA dumps cost a few seconds of compiler time.","title":"Resource report and instruction mix"},{"category":"section","location":"resources_mix/#Static-instruction-mix-(native-or-cross-compiled)","page":"Resource report and instruction mix","text":"m = kernel_instruction_mix(backend, only(cks))                 # this device's code\nm.counts.fp64_fma, m.counts.fp64_add, m.counts.fp64_mul         # whole binary, every path once\nm.hot_loop.counts, m.hot_loop_confidence                        # the per-slot loop: one pass, nested loops once\nm942 = kernel_instruction_mix(backend, only(cks); target = \"gfx942\")   # the MI300X code, without an MI300X\nm90 = kernel_instruction_mix(backend, only(cks); target = \"sm_90\")     # the H100 code, from any CUDA context\nfp64_issue_floor(m; n_slots = n_work_items * n_iterations_per_item, peak_fp64_flops = measure_peak_flops(backend),\n    kernel_time_s = median_launch_s)                            # FP64-pipe time floor and fraction\n\nThe disassembly (AMD ISA text from code_native; NVIDIA SASS from CUDA.jl's bundled nvdisasm on the cubin) is counted by class — FP64 fma / add / mul / transcendental seeds / other / CDNA packed, FP32, integer, scalar, memory loads / stores / atomics, constant loads, LDS, control, waits, nops, other (MIX_CLASSES) — for the whole kernel and for every loop of its control-flow graph (natural loops from dominators), the hot loop being the outermost loop with the most instructions. On AMD the loop nest is checked against the LLVM assembly printer's own loop annotations (:high confidence when they agree); SASS has none, so the CFG result stands alone (:medium with a single dominant outer loop). target = \"gfx942\" / \"sm_90\" compiles the same kernel — same function, argument types, always_inline, static workgroup size — for another architecture through GPUCompiler, so the code a rented machine will run can be read before renting it (e.g. whether the CDNA3 compiler emits v_pk_fma_f64 at all — for the scalar FP64 kernels this was developed on it does not; or that ptxas pads an sm120 hot loop with NOPs where the sm90 loop has none). Static counts count code, not execution; against Nsight Compute on an RTX 5090 the hot-loop DFMA / DADD+DMUL counts of an FP64 Newton-iteration kernel reproduced the measured per-slot warp-instruction counts exactly. fp64_issue_floor divides a per-slot FP64 count × executed slots by the measured FP64 lane-instruction rate (measure_peak_flops / 2 — an FMA is 2 FLOP): the time the FP64 pipe alone needs per launch, assuming every FP64 instruction issues at the FMA rate.\n\nThe classifiers are ordered rule tables, SASS_RULES / AMD_RULES :: Vector{Pair{Regex, Symbol}}, built on the mnemonic grammars (SASS: the leading operand-type letter D/F/H/I/U, the LD/ST/ATOM/RED + G/L/S/C memory suffixes; AMD: the v_/s_/ds_/global_ prefixes and _f64/_f32 suffixes) with a short exception list ahead of each generic rule; the last rule is a catch-all whose hits are other but reported as unclassified / unclassified_opcodes / coverage (1.0 on all four validated targets). The vectors are mutable — pushfirst!(SASS_RULES, r\"^MYOP\" => :int) overrides for the session. kernel_ir_mix (also the ir field of kernel_instruction_mix) walks the OPTIMIZED LLVM IR of the same job with LLVM.jl, typed by opcode and operand type (with the contract fast-math flag counted as fp64_contract: Julia lowers muladd to a contract-flagged fmul/fadd pair, which the AMD backend fuses only at instruction selection) (IR_CLASSES: fp64 fma / add / mul / div / neg / sqrt / cmp / cvt / intrinsic, fp32, int, memory, call, control, other) — the arithmetic before the backend: IR fma 0 against 132 v_fma_f64 / 301 DFMA is the backend's FMA contraction, IR div 14 / sqrt 3 against the machine's reciprocal seeds + FMA sequences its expansion. Whole-module totals (no IR loop attribution).\n\nGotchas: CUDA.jl's code_sass loads the module on the current device (via CUPTI), which an sm_90 cubin cannot do on an sm_120 GPU — the extension compiles with CUDACore.compile and runs nvdisasm on the image directly, so no CUPTI and no device of the target architecture are needed. AMDGPU.jl caches the OCLC ISA-version device library under an ISA-agnostic key; the extension evicts it around a cross-compile so gfx942 links oclc_isa_version_942, not the current device's.","title":"Static instruction mix (native or cross-compiled)"},{"category":"section","location":"peaks_probes/#Measured-peak-and-cheap-probes","page":"Measured peak and cheap probes","text":"measure_peak_flops(backend)            # FP64 FLOP/s, dependent-FMA-chain kernel, best of 5\nmeasure_peak_flops(backend, Float32)   # the FP32 rate of the same probe\n\nNo per-architecture table: the probe measures the attainable vector rate at the clocks the device actually holds, and is never routed to matrix/tensor units (the wrong yardstick for scalar kernels). The result is checked against a host reference so a mis-launched kernel cannot be credited. It runs on the CPU backend too, one scalar chain per work-item, which under-reports the host by its SIMD width; the vectorised host number is LinearAlgebra.peakflops(2048; ntrials = 3), a different quantity.","title":"Measured peak and cheap probes"},{"category":"section","location":"peaks_probes/#Cheap-probes","page":"Measured peak and cheap probes","text":"measure_launch_overhead(backend)   # LaunchOverhead: device / enqueue / round-trip µs per launch, queue depth\nhost_snapshot(backend)             # Julia + BLAS threads vs host cores AND the cgroup quota, memory limits,\n                                   # OS kernel, driver / runtime / vendor package versions, warnings as data\nfirst_launch_s(timer)              # the JIT-carrying first launch per device; launch_times(timer; skip_first = true)\n\nThe launch probe sets the floor below which launches dominate (it differs by an order of magnitude between drivers, and between a VM and bare metal). The host snapshot is the first section of any report: two manifests written months apart compare only if they say what they ran on, and the \"pod sees the node's cores while the cgroup grants a fraction\" trap is invisible in every device counter.","title":"Cheap probes"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"\n\nThe backend_* hooks are documented on the Porting a backend page.","title":"API reference"},{"category":"module","location":"api/#GPUDiagnostics.GPUDiagnostics","page":"API reference","text":"GPUDiagnostics\n\nVendor-neutral runtime diagnostics for KernelAbstractions code. Every entry point dispatches on the KA Backend; the CUDA.jl / AMDGPU.jl package extensions supply the vendor methods, loaded on demand when the vendor package is in the session, and the KA CPU backend gets host fallbacks so the plumbing (and its tests) run without a GPU.\n\nCapabilities — supports(backend, :feature) / capabilities(backend) say which of the subsystems below a backend implements (FEATURES); an entry point of an undeclared feature throws BackendUnsupported naming the feature and, when known, the package to load.\nDevice API — gpu_device_count, gpu_device, gpu_device!, gpu_name, gpu_arch, gpu_sm_count, gpu_max_threads_per_sm, gpu_memory_info, gpu_power, gpu_utilization, thread_fill_occupancy. KernelAbstractions has no device management of its own.\nDevice-event kernel timing — gpu_event / gpu_elapsed on the task-local launch stream, and LaunchTimer + launch_times for one event pair per launch: the only kernel clock that works when launches are queued asynchronously (a host clock measures enqueue latency).\nTelemetry — gpu_sample is one snapshot of a device (power / utilization / VRAM / SM and memory clocks / edge and hotspot temperature / power limit / throttle-reason bitmask — decoded by throttle_reasons — plus NVIDIA GPM hardware counters where available: ACHIEVED SM occupancy, FP64 / FP32 / tensor pipe and DRAM-bandwidth utilization, PCIe / NVLink traffic); with_gpu_sampler runs a function while a child process takes that sample per device per tick into a TSV, and gpu_telemetry_stats reduces the resulting GPUTelemetry column table (means, peaks, busy-window means and medians, the power-capped fraction). In-process sampling wedges behind a backed-up kernel stream or is suspended by Julia's GC/timer coupling; a child is immune.\nMeasured peak — measure_peak_flops(backend, T): a dependent-FMA-chain kernel gives the attainable vector FP64 (or FP32) rate of the device at the clocks it actually holds, never routed to matrix/tensor units. Runs on every backend, the CPU included.\nCompile-time resource report — compiled_kernels inventories the kernels this process compiled (from the vendor's kernel cache, so closures inside driver functions are reachable too) and kernel_resources reports registers, spill/stack and shared (LDS) memory, and the runtime's theoretical occupancy for the block size actually launched — plus the SGPR/VGPR/ spill counts from the AMD ISA dump.\nStatic instruction mix — kernel_instruction_mix counts the compiled instruction stream (AMD ISA / NVIDIA SASS) by class — FP64 fma/add/mul/transcendental/other/packed, FP32, integer, scalar, memory, LDS, control, waits — for the whole kernel and for each loop of its control-flow graph (the hot per-slot loop in particular), natively or cross-compiled for a target that is not present (target = \"gfx942\", \"sm_90\"); kernel_ir_mix counts the same job's optimized LLVM IR by typed opcode (the arithmetic before backend contraction); fp64_issue_floor turns a per-slot FP64 count and the measured FP64 rate into the FP64-pipe time floor per launch.\nCheap probes — measure_launch_overhead (device / enqueue / round-trip cost of a launch: the floor below which launches dominate) and host_snapshot (Julia and BLAS threads against the host cores AND the cgroup CPU quota, memory limits, OS kernel, driver / runtime / vendor package versions, warnings as data); first_launch_s / launch_times(; skip_first) separate the JIT-carrying first launch from the steady state.\nReport layer — diagnostics_dict(x; prefix) turns every result type (KernelResources, KernelInstructionMix, IRMix, FP64IssueFloor, LaunchTimer, GPUTelemetry, HWCounters) into a flat Dict{String, Any} of TOML-safe scalars with stable, additive keys (a value the backend could not report is omitted; every dict carries gpudiagnostics_schema), and each result type prints as a readable summary. GPUTelemetry implements Tables.jl, so DataFrames / CSV.jl consume it directly.\nPer-dispatch hardware counters — hw_counter_command wraps a process in rocprofv3 or Nsight Compute. hw_counters parses raw CSV into HWCounters, retaining each dispatch's identity, resources, device, native counters and units. hw_counter_derived returns aligned derived columns; hw_counter_summary / diagnostics_dict explicitly reduce them. Parsing needs no GPU or vendor package. Sampled counters remain in GPUTelemetry.\n\n\n\n\n\n","title":"GPUDiagnostics.GPUDiagnostics"},{"category":"constant","location":"api/#GPUDiagnostics.AMD_RULES","page":"API reference","text":"AMD_RULES :: Vector{Pair{Regex, Symbol}}\n\nOrdered classification rules of instruction_mix for AMD ISA mnemonics (GCN / RDNA / CDNA), matched after the _e32/_e64/_dpp/_sdwa encoding suffix is dropped. Same contract as SASS_RULES: first match wins, the final rule is the :unclassified catch-all, and the vector is mutable for session overrides.\n\n\n\n\n\n","title":"GPUDiagnostics.AMD_RULES"},{"category":"constant","location":"api/#GPUDiagnostics.COUNTER_SETS","page":"API reference","text":"COUNTER_SETS\n\nIntent-based presets: COUNTER_SETS[:amd][:issue] / COUNTER_SETS[:nvidia][:issue], plus :occupancy, :memory, :fp64, and :l2. Each value is a CounterSet whose notes say what the preset answers, how its raw values must be read (which counters are per-wave, per-thread, event counts or quad-cycles) and the trap specific to it — read them before interpreting a collection. AMD sets are validated on gfx942 with rocprofv3 1.1.0, ROCm 7.2.4, and each fits one pass there; the L2 set is at the TCC block's four-counter capacity, and exceeding a block's capacity aborts rocprofv3 and hangs the workload (hence timeout -k in hw_counter_command). NVIDIA presets were collected on sm_120 with ncu 2025.4.1; their recorded pass counts apply to a bounded FMA probe only, not to arbitrary workloads or other GPUs. Raw custom metrics remain available. Unsupported metrics are errors from the collector, never silently replaced with different measurements.\n\n\n\n\n\n","title":"GPUDiagnostics.COUNTER_SETS"},{"category":"constant","location":"api/#GPUDiagnostics.FEATURES","page":"API reference","text":"FEATURES\n\nThe capability names a backend may declare through supports, each naming one subsystem of the package:\n\nfeature entry points\n:devices gpu_device_count, gpu_device, gpu_device!, gpu_name, gpu_arch\n:device_props gpu_sm_count, gpu_max_threads_per_sm, gpu_memory_info, thread_fill_occupancy\n:events gpu_event, gpu_elapsed, LaunchTimer\n:telemetry gpu_power, gpu_utilization, gpu_sample, gpu_sampler_sources, with_gpu_sampler\n:telemetry_counters hardware counters in the sample (NVIDIA GPM: achieved occupancy, pipe utilizations, …)\n:peak_flops measure_peak_flops (the FMA-chain probe runs on this backend)\n:fp64 the device computes in Float64: measure_peak_flops(b, Float64), fp64_issue_floor\n:kernel_inventory compiled_kernels\n:resources kernel_resources (registers, spill/local and shared memory)\n:occupancy the runtime occupancy calculator behind kernel_resources\n:native_mix kernel_instruction_mix (a disassembly of the device code exists)\n:ir_mix kernel_ir_mix\n:hw_counters hw_counter_command, hw_counter_status (rocprofv3 on AMD, ncu on NVIDIA)\n\n\n\n\n\n","title":"GPUDiagnostics.FEATURES"},{"category":"constant","location":"api/#GPUDiagnostics.GPUDIAGNOSTICS_SCHEMA","page":"API reference","text":"GPUDIAGNOSTICS_SCHEMA\n\nVersion of the key layout diagnostics_dict writes. Every dict carries it under \"gpudiagnostics_schema\" (unprefixed, so a manifest merging several dicts holds it once). Keys are additive within a schema; version 2 introduces the unified hardware-counter layout.\n\n\n\n\n\n","title":"GPUDiagnostics.GPUDIAGNOSTICS_SCHEMA"},{"category":"constant","location":"api/#GPUDiagnostics.IR_CLASSES","page":"API reference","text":"IR_CLASSES\n\nOperation classes of kernel_ir_mix: fp64_fma (llvm.fma.f64, llvm.fmuladd.f64), fp64_add (fadd/fsub double), fp64_mul, fp64_div, fp64_neg, fp64_sqrt (llvm.sqrt.f64), fp64_cmp (fcmp on doubles), fp64_cvt (conversions to/from double), fp64_intrinsic (other llvm.* intrinsics returning or taking double: fabs, floor, minnum, …), fp64_contract (the subset of the double fadd/fsub/fmul that carry LLVM's contract fast-math flag — Julia lowers muladd to a contract-flagged pair rather than an fmuladd intrinsic, and the backend fuses them at instruction selection; informational, not added to the fp64 total), fp32 (the same on float/half), int (integer arithmetic, logic, shifts, compares, integer casts, selects), mem_load, mem_store, mem_atomic, call (non-intrinsic calls), control (br, switch, ret, unreachable), other (phi, getelementptr, allocas, pointer casts, extract/insertvalue, …).\n\n\n\n\n\n","title":"GPUDiagnostics.IR_CLASSES"},{"category":"constant","location":"api/#GPUDiagnostics.MIX_CLASSES","page":"API reference","text":"MIX_CLASSES\n\nInstruction classes of kernel_instruction_mix (vendor-neutral; the manifest keys are kernel_mix_<class>):\n\nfp64_fma, fp64_add, fp64_mul — FP64 fused multiply-add (DFMA / v_fma_f64, v_fmac_f64, v_div_fmas_f64), add/sub, mul;\nfp64_trans — FP64 reciprocal / rsqrt / sqrt seeds (MUFU.RCP64H, MUFU.RSQ64H / v_rcp_f64, v_rsq_f64, v_sqrt_f64);\nfp64_other — every other FP64-pipe instruction: compares (DSETP / v_cmp_*_f64), min/max, conversions to/from FP64, rounding, the division helpers (v_div_scale_f64, v_div_fixup_f64, v_ldexp_f64);\nfp64_packed — CDNA packed FP64 (v_pk_fma_f64, v_pk_mul_f64, v_pk_add_f64: two lanes of FP64 work per instruction; NVIDIA has no equivalent);\natomic_fallback — every instruction inside the address-space fallback paths LLVM's AtomicExpand pass emits for an atomic on a GENERIC (flat) pointer: the %atomicrmw.private* and %atomicrmw.shared* machine blocks (identified by the assembly printer's block names on AMD), i.e. the scratch_load/store pair and the ds_* path that a device-memory atomic never executes. Counted here instead of as mem_load / mem_store / lds, which would inflate the memory classes and suggest register spills that do not exist (the kernel's scratch size is 0). Nonzero means the kernel's atomics take a generic pointer — an address-space-1 pointer removes the paths altogether. Not emitted for SASS (no block names; NVIDIA lowers generic atomics without such paths);\nfp32 — FP32 and FP16 arithmetic (FFMA/FADD/FMUL/MUFU.RCP/HFMA2 / v_*_f32, v_*_f16);\nint — vector integer / logic / move / select / compare (IMAD, IADD3, LOP3, SHF, ISETP, MOV, SEL, PRMT … / v_add_co_u32, v_cndmask_b32, v_mov_b32, v_readlane_b32, v_accvgpr_* …);\nsalu — scalar / uniform-datapath instructions (AMD s_* ALU; NVIDIA U* uniform ops, R2UR, S2UR);\nmem_load, mem_store, mem_atomic — vector-memory loads, stores, atomics (global, flat, local/scratch, buffer);\nsmem — scalar / constant-bank loads (s_load_*; LDC, LDCU, ULDC);\nlds — shared memory (ds_*; LDS, STS, ATOMS, LDSM);\ncontrol — branches, calls, returns, exit, convergence (BSSY/BSYNC/WARPSYNC/BREAK) and barriers;\nwait — explicit dependency waits and software-scheduled stalls (s_waitcnt*, s_wait_*, RDNA3's s_delay_alu; DEPBAR);\nnop — no-ops that occupy issue slots (s_nop, v_nop; NOP — ptxas pads FP64 dependency latency with them on the 64:1-rate consumer parts, so they can be a large share of a loop);\nother — scheduling hints (s_clause, s_set_inst_prefetch_distance), cache maintenance and fences (buffer_gl*_inv, buffer_wbl2, MEMBAR, ERRBAR, CCTL), special registers (S2R, CS2R), shuffles/votes, and anything unrecognised (see the opcodes histogram of the report).\n\n\n\n\n\n","title":"GPUDiagnostics.MIX_CLASSES"},{"category":"constant","location":"api/#GPUDiagnostics.SASS_RULES","page":"API reference","text":"SASS_RULES :: Vector{Pair{Regex, Symbol}}\n\nOrdered classification rules of instruction_mix for NVIDIA SASS mnemonics (matched against the full OPC.MOD… opcode) and AMD ISA mnemonics (matched after the _e32/_e64/ _dpp/_sdwa encoding suffix is dropped). The first matching rule wins; the final r\"\" rule is the catch-all :unclassified (counted as other, listed in unclassified_opcodes). The vectors are mutable: pushfirst!(SASS_RULES, r\"^MYOP\" => :int) overrides for a session.\n\n\n\n\n\n","title":"GPUDiagnostics.SASS_RULES"},{"category":"type","location":"api/#GPUDiagnostics.BackendUnsupported","page":"API reference","text":"BackendUnsupported(backend, feature, entry)\n\nThrown by an entry point (entry, the function's name) when backend does not declare feature through supports: either the backend cannot provide it (the CPU backend has no telemetry, a device without FP64 has no FP64 peak) or the vendor extension that would provide it is not loaded — the message says which package that is when it is known.\n\n\n\n\n\n","title":"GPUDiagnostics.BackendUnsupported"},{"category":"type","location":"api/#GPUDiagnostics.CompiledKernel","page":"API reference","text":"CompiledKernel\n\nOne entry of compiled_kernels: a kernel this process compiled for the vendor runtime. name is the Julia function the kernel was generated from (gpu__forindices_global! for an AcceleratedKernels foreachindex, gpu_<kernel> for a KernelAbstractions @kernel), signature the argument-type tuple as a string (closure types carry the enclosing function's name on Julia ≥ 1.12), workgroup_size the static KernelAbstractions workgroup size read from the signature (nothing when dynamic or not a KA kernel), and kernel the vendor kernel object (CUDA.jl HostKernel, AMDGPU.jl HIPKernel).\n\n\n\n\n\n","title":"GPUDiagnostics.CompiledKernel"},{"category":"type","location":"api/#GPUDiagnostics.CounterSet","page":"API reference","text":"CounterSet\n\nA profiler preset: native metrics, the validated_on architecture/tool descriptions, passes (known only in that validation context), and notes. Presets express intent; they do not promise identical counters or collection cost on different architectures.\n\n\n\n\n\n","title":"GPUDiagnostics.CounterSet"},{"category":"type","location":"api/#GPUDiagnostics.FP64IssueFloor","page":"API reference","text":"FP64IssueFloor\n\nResult of fp64_issue_floor: scope, fp64_per_slot, n_slots, fp64_lane_instructions, peak_fp64_flops, floor_s, kernel_time_s and fp64_issue_fraction (both missing when no launch time was given), confidence and the assumptions the floor rests on.\n\n\n\n\n\n","title":"GPUDiagnostics.FP64IssueFloor"},{"category":"type","location":"api/#GPUDiagnostics.GPUTelemetry","page":"API reference","text":"GPUTelemetry\n\nColumn table returned by with_gpu_sampler: columns ([:t_rel_s, :device, metric columns…] as declared by the child), samples::Matrix{Float64} (one row per device per tick, NaN where a metric is not reported), ticks (sample rounds = rows of the first device), dt and window (requested cadence / sampled window, s), first_sample_s (child startup lag; missing when no sample landed), starved (ticks ≪ window/dt ⇒ stats unreliable), trace (the TSV, or nothing) and counters. Index a column by symbol: telem[:compute_util]; haskey(telem, :fp64_util).\n\n\n\n\n\n","title":"GPUDiagnostics.GPUTelemetry"},{"category":"type","location":"api/#GPUDiagnostics.HWCounterAvailability","page":"API reference","text":"HWCounterAvailability\n\nTool discovery, separate from permission to collect: tool, executable (path or missing), available::Bool, and permitted::Union{Missing,Bool}. A cheap discovery check leaves permitted = missing; only an actual profiling run establishes access.\n\n\n\n\n\n","title":"GPUDiagnostics.HWCounterAvailability"},{"category":"type","location":"api/#GPUDiagnostics.HWCounterCollector","page":"API reference","text":"HWCounterCollector\n\nExternal profiler selected by a backend extension. Use RocprofV3 or NsightCompute explicitly when preparing an external workload without a GPU runtime.\n\n\n\n\n\n","title":"GPUDiagnostics.HWCounterCollector"},{"category":"type","location":"api/#GPUDiagnostics.HWCounters","page":"API reference","text":"HWCounters\n\nPortable per-dispatch counter data for one selected kernel. dispatches aligns with rows of values; counters names its columns, and units retains each native metric's unit (missing where the tool does not supply one). Index by native name: hc[\"SQ_WAVES\"]. vendor, tool, name, kernel, and provenance describe the collection. Parsers never load a GPU package, run a profiler, or silently summarize launches. Use hw_counter_derived for derived columns and hw_counter_summary for reduction.\n\n\n\n\n\n","title":"GPUDiagnostics.HWCounters"},{"category":"type","location":"api/#GPUDiagnostics.HWDispatch","page":"API reference","text":"HWDispatch\n\nIdentity and metadata for one logical profiled launch. id is tool-local, so identify a dispatch with (process_id, device_id, context_id, queue_id, id). Numeric IDs retain the profiler's indexing. start_s is in the tool's clock domain, not a host epoch. duration_s is a profiled duration, potentially assembled over replay passes. resources and device contain native metadata for THIS dispatch. Unavailable fields are missing; slots is the caller's work-item iteration count for this launch, not inferred from the grid.\n\n\n\n\n\n","title":"GPUDiagnostics.HWDispatch"},{"category":"type","location":"api/#GPUDiagnostics.HostSnapshot","page":"API reference","text":"HostSnapshot\n\nResult of host_snapshot: what the run happened on, for comparing manifests written months apart and for the host-side traps no device counter shows.\n\njulia, julia_threads, blas_threads, os, kernel (OS kernel release), hostname;\ncpu_threads (what Julia sees) and cpu_quota (the cgroup CPU quota in cores, missing when unlimited or not on Linux) — a pod sees the node's cores while the cgroup grants a fraction;\nmemory_total_B, memory_available_B, memory_limit_B (cgroup limit, missing when none);\nbackend, gpu_driver, gpu_runtime, gpu_kernel_module (the nvidia / amdgpu module version), gpu_package (from the vendor extension), packages (versions of GPUDiagnostics and KernelAbstractions);\nwarnings::Vector{String} — findings, as data: Julia threads over the cgroup quota, or Julia × BLAS threads over the host cores.\n\n\n\n\n\n","title":"GPUDiagnostics.HostSnapshot"},{"category":"type","location":"api/#GPUDiagnostics.IRMix","page":"API reference","text":"IRMix\n\nResult of kernel_ir_mix: target, total, fp64, counts (over IR_CLASSES) and functions (Dict name ⇒ counts) of the optimized LLVM IR of a kernel's job.\n\n\n\n\n\n","title":"GPUDiagnostics.IRMix"},{"category":"type","location":"api/#GPUDiagnostics.InstructionMix","page":"API reference","text":"InstructionMix\n\nResult of instruction_mix: the static instruction counts of one disassembly by class (counts over MIX_CLASSES, total, fp64), the per-opcode histogram (opcodes), the classifier's coverage (unclassified, unclassified_opcodes, coverage), the parsed block count and the loop nest (loops::Vector{MixLoop}, hot_loop::Union{Nothing, MixLoop}, hot_loop_confidence, llvm_loops_agree::Union{Missing, Bool}). See instruction_mix for the meaning of each field.\n\n\n\n\n\n","title":"GPUDiagnostics.InstructionMix"},{"category":"type","location":"api/#GPUDiagnostics.KernelInstructionMix","page":"API reference","text":"KernelInstructionMix\n\nResult of kernel_instruction_mix: the kernel's name and signature, the target ISA the counted code was compiled for, native (whether that is the current device's), registers (from the AMD listing; missing for SASS), the InstructionMix itself as mix, and ir::Union{Nothing, IRMix}. Every field of mix is also readable directly (m.counts, m.hot_loop, …).\n\n\n\n\n\n","title":"GPUDiagnostics.KernelInstructionMix"},{"category":"type","location":"api/#GPUDiagnostics.KernelOccupancy","page":"API reference","text":"KernelOccupancy\n\nThe occupancy block of a KernelResources: the vendor occupancy calculator's active_blocks_per_sm at the report's block size, converted to active_warps_per_sm (waves on AMD) and divided by the device's max_warps_per_sm into fraction ∈ (0, 1] — the theoretical occupancy the launch can reach, accounting for registers, shared memory and block-size granularity together. warp_size, max_threads_per_sm and shared_mem_per_sm are the device capacities it is measured against (shared_mem_per_sm ÷ shared_mem_bytes blocks is the shared-memory bound). SM = CU / WGP on AMD.\n\n\n\n\n\n","title":"GPUDiagnostics.KernelOccupancy"},{"category":"type","location":"api/#GPUDiagnostics.KernelResources","page":"API reference","text":"KernelResources\n\nThe compile-time resource report of kernel_resources for one kernel at one block size:\n\nname, signature, block_size — identification and the block size the occupancy is for;\nregisters — architectural registers per thread (NVIDIA registers; AMD VGPRs);\nlocal_mem_bytes — per-thread stack/spill memory (NVIDIA local memory; AMD scratch, the private segment). Non-zero means register spills or a stack frame: every access is a global-memory round trip;\nshared_mem_bytes — static shared memory (LDS) per block the kernel descriptor RESERVES, whether or not the source declares any (LLVM's AMDGPU backend promotes private arrays it cannot keep in registers to LDS, sized for the kernel's maximum block size);\nconst_mem_bytes, max_threads_per_block — as reported by the runtime;\noccupancy — a KernelOccupancy, or missing when the backend has no occupancy calculator (:occupancy capability) or reports no resident-warp capacity;\nisa — a Dict{String, Any} of extra figures read from the compiled code where the vendor exposes them (see kernel_resources).\n\nEvery count the runtime cannot report is missing.\n\n\n\n\n\n","title":"GPUDiagnostics.KernelResources"},{"category":"type","location":"api/#GPUDiagnostics.LaunchOverhead","page":"API reference","text":"LaunchOverhead\n\nResult of measure_launch_overhead, all medians over n launches of a one-instruction kernel, in seconds: device_s (device-event time per launch: the smallest kernel the device can run PLUS the cost of the event pair itself, a few µs, so for an empty kernel it is an upper bound and may exceed roundtrip_s), enqueue_s (host time to enqueue one asynchronous launch), roundtrip_s (host time of one launch followed by a synchronize — the cost of a per-launch sync). queue_depth is roundtrip_s / enqueue_s: how many launches the host can queue in the time one takes to complete, i.e. how far a launch loop can run ahead before it stalls.\n\n\n\n\n\n","title":"GPUDiagnostics.LaunchOverhead"},{"category":"type","location":"api/#GPUDiagnostics.LaunchTimer","page":"API reference","text":"LaunchTimer()\n\nCollects a device-event pair per kernel launch (see gpu_event), keyed by the 1-based device the launch ran on. A launch loop instruments itself with launch_lane once per call and launch_tick / launch_tock! around each launch; read back with launch_times once the loop has returned. Safe to share across per-device tasks (pushes are locked; events are per-stream). Passing nothing instead of a timer records nothing and costs nothing.\n\nlane = launch_lane(timer, backend)\nfor item in work\n    e0 = launch_tick(timer, backend)\n    launch_kernel!(item, backend)\n    launch_tock!(timer, lane, backend, e0)\nend\nlaunch_times(timer)   # Dict(device => [seconds per launch, …])\n\n\n\n\n\n","title":"GPUDiagnostics.LaunchTimer"},{"category":"type","location":"api/#GPUDiagnostics.MixLoop","page":"API reference","text":"MixLoop\n\nOne natural loop of a kernel's control-flow graph, as listed in InstructionMix.loops: header (block label), depth (1 = outermost), blocks, total and counts (everything inside the loop, nested loops included), exclusive_total / exclusive_counts (the loop's own blocks only).\n\n\n\n\n\n","title":"GPUDiagnostics.MixLoop"},{"category":"type","location":"api/#GPUDiagnostics.NsightCompute","page":"API reference","text":"NsightCompute()\n\nNsight Compute collector for an external NVIDIA workload. The CUDA extension selects this automatically for CUDABackend(). Use it explicitly with hw_counter_command or hw_counter_status when no CUDA runtime is loaded.\n\n\n\n\n\n","title":"GPUDiagnostics.NsightCompute"},{"category":"type","location":"api/#GPUDiagnostics.RocprofV3","page":"API reference","text":"RocprofV3()\n\nrocprofv3 collector for an external AMD workload. The AMDGPU extension selects this automatically for ROCBackend(). Use it explicitly with hw_counter_command or hw_counter_status when no AMDGPU runtime is loaded.\n\n\n\n\n\n","title":"GPUDiagnostics.RocprofV3"},{"category":"type","location":"api/#GPUDiagnostics.SamplerSource","page":"API reference","text":"SamplerSource\n\nPer-device sampling state. Subtypes implement sample!(source) -> NamedTuple (metric name ⇒ Float64, NaN for a metric the device does not report) and carry a device::Int field (the 1-based vendor ordinal); close!(source) releases resources (default no-op). Sources are built from string specs by sampler_source: <kind>:<payload>, dispatched on Val(kind).\n\n\n\n\n\n","title":"GPUDiagnostics.SamplerSource"},{"category":"method","location":"api/#GPUDiagnostics._natural_loops-Tuple{Vector{GPUDiagnostics.MixBlock}}","page":"API reference","text":"_natural_loops(blocks) -> Vector{(header, body, depth)}\n\nNatural loops of the block graph: one per loop header (back edges u → h with h dominating u, bodies merged per header), with the nesting depth (1 = outermost). Returns block indices.\n\n\n\n\n\n","title":"GPUDiagnostics._natural_loops"},{"category":"method","location":"api/#GPUDiagnostics.capabilities-Tuple{KernelAbstractions.Backend}","page":"API reference","text":"capabilities(backend) -> Vector{Symbol}\n\nThe features of FEATURES that backend declares through supports, in FEATURES order.\n\n\n\n\n\n","title":"GPUDiagnostics.capabilities"},{"category":"method","location":"api/#GPUDiagnostics.compiled_kernels-Tuple{KernelAbstractions.Backend}","page":"API reference","text":"compiled_kernels(backend; pattern = nothing) -> Vector{CompiledKernel}\n\nInventory of the kernels THIS PROCESS has compiled for backend's vendor runtime, from the vendor package's kernel-instance cache (every @cuda/@roc/KernelAbstractions launch lands there on its first call). pattern (a Regex or AbstractString) keeps only entries whose name or signature matches — the way to pick a production kernel that is a closure inside its driver: compiled_kernels(backend; pattern = r\"_my_driver!\"). The CPU backend compiles nothing and returns an empty vector. Pass entries to kernel_resources.\n\n\n\n\n\n","title":"GPUDiagnostics.compiled_kernels"},{"category":"function","location":"api/#GPUDiagnostics.diagnostics_dict","page":"API reference","text":"diagnostics_dict(x; prefix = \"\") -> Dict{String, Any}\n\nFlat, TOML-safe dictionary of a result — KernelResources, KernelInstructionMix / InstructionMix, IRMix, FP64IssueFloor, LaunchTimer, GPUTelemetry, HWCounters — with every key prefixed by prefix (the convention used by the first consumer: kernel_, kernel_mix_, kernel_ir_, sampler_, hw_), ready to merge! into a run manifest. Values are Int, Float64, Bool, String or vectors of those; Symbols become strings; a missing value is omitted (never written as a sentinel), as are nothing fields. Every dict also carries \"gpudiagnostics_schema\" =>GPUDIAGNOSTICS_SCHEMA.\n\nThe instruction-mix dict does not include the IR counts: call diagnostics_dict(m.ir; prefix = \"kernel_ir_\") for those, so the two key families stay separately prefixed.\n\n\n\n\n\n","title":"GPUDiagnostics.diagnostics_dict"},{"category":"method","location":"api/#GPUDiagnostics.first_launch_s-Tuple{LaunchTimer}","page":"API reference","text":"first_launch_s(timer) -> Dict{Int, Float64}\n\nDevice time of the FIRST launch on each device — the one that carries the JIT compile when the kernel was not compiled before the loop. launch_times(timer; skip_first = true) drops it, so the steady-state statistics are not dragged by the warm-up.\n\n\n\n\n\n","title":"GPUDiagnostics.first_launch_s"},{"category":"method","location":"api/#GPUDiagnostics.fp64_issue_floor-Tuple{Any}","page":"API reference","text":"fp64_issue_floor(mix; n_slots, peak_fp64_flops, kernel_time_s = nothing, scope = :hot_loop) -> FP64IssueFloor\n\nThe time the FP64 pipe alone needs for a launch, from a STATIC instruction count and a MEASURED issue rate: fp64_per_slot FP64 instructions (the six FP64 classes of mix's hot_loop — scope = :total uses the whole-kernel count instead) × n_slots executions (hot-loop iterations summed over all threads; e.g. pixels × samples when every slot is inside the window) ÷ the device's FP64 lane-instruction rate, taken as peak_fp64_flops / 2 (measure_peak_flops runs an FMA chain: 2 FLOP per instruction). floor_s is a lower bound on the launch time on an FP64-issue-bound device; fp64_issue_fraction = floor_s / kernel_time_s (when a measured launch time is given) is the fraction of the launch during which the FP64 pipe was provably issuing (missing, like kernel_time_s, when no launch time was given).\n\nAssumptions, stated in assumptions: every FP64 instruction issues at the FMA rate (adds and multiplies do; MUFU/v_rcp_f64 seeds and packed CDNA ops do not, so the floor errs low); the static hot-loop count is one pass through the loop with nested loops counted once — an inner loop of n iterations (a Newton solve, say) adds (n − 1) × its own count per slot, so the floor is again low by that much; nothing outside the hot loop (per-thread setup) is counted.\n\n\n\n\n\n","title":"GPUDiagnostics.fp64_issue_floor"},{"category":"function","location":"api/#GPUDiagnostics.gpu_arch","page":"API reference","text":"gpu_arch(backend) -> String\n\nArchitecture tag of the current device: the compute capability (\"9.0\") on NVIDIA, the gfx name without feature suffixes (\"gfx942\") on AMD. Provenance only.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_arch"},{"category":"function","location":"api/#GPUDiagnostics.gpu_device","page":"API reference","text":"gpu_device(backend) -> Int\n\n1-based index of the current device (this common API is 1-based across vendors).\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_device"},{"category":"function","location":"api/#GPUDiagnostics.gpu_device!","page":"API reference","text":"gpu_device!(backend, i) -> Int\n\nMake GPU i (1-based) current; returns the previously-current index. One Julia task per device\n\ngpu_device! is how multi-device work sharding pins each shard to a GPU.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_device!"},{"category":"function","location":"api/#GPUDiagnostics.gpu_device_count","page":"API reference","text":"gpu_device_count(backend) -> Int\n\nNumber of GPUs the vendor runtime exposes for backend.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_device_count"},{"category":"function","location":"api/#GPUDiagnostics.gpu_elapsed","page":"API reference","text":"gpu_elapsed(start, stop) -> Float64\n\nSeconds between two events from gpu_event. Waits for stop to complete first, so it is safe to call before the stream is otherwise synchronized.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_elapsed"},{"category":"function","location":"api/#GPUDiagnostics.gpu_event","page":"API reference","text":"gpu_event(backend) -> event\n\nRecord a timestamp event on the CURRENT TASK's stream (the one KernelAbstractions launches on) and return it. Pair two with gpu_elapsed. The CPU backend returns time_ns() — its kernels are synchronous, so the host clock is the kernel clock.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_event"},{"category":"function","location":"api/#GPUDiagnostics.gpu_max_threads_per_sm","page":"API reference","text":"gpu_max_threads_per_sm(backend) -> Int\n\nMax resident threads per SM/CU — with gpu_sm_count, the device's total resident-thread capacity (the denominator of thread-fill occupancy).\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_max_threads_per_sm"},{"category":"function","location":"api/#GPUDiagnostics.gpu_memory_info","page":"API reference","text":"gpu_memory_info(backend) -> @NamedTuple{total, free, used}\n\nCurrent-device global memory, in bytes.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_memory_info"},{"category":"function","location":"api/#GPUDiagnostics.gpu_name","page":"API reference","text":"gpu_name(backend) -> String\n\nMarketing name of the current device.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_name"},{"category":"function","location":"api/#GPUDiagnostics.gpu_power","page":"API reference","text":"gpu_power(backend) -> Float64\n\nInstantaneous board power draw of the current device, in Watts. A cheap live proxy for compute saturation — NOT occupancy: low power + high SM-utilization ⇒ latency-bound.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_power"},{"category":"function","location":"api/#GPUDiagnostics.gpu_sample","page":"API reference","text":"gpu_sample(backend, device = gpu_device(backend); counters = :auto) -> NamedTuple\n\nOne telemetry sample of device (1-based vendor ordinal), in-process: power_W, compute_util, mem_util, vram_used_B, sm_clock_MHz, mem_clock_MHz, temperature_C, hotspot_C, power_limit_W, throttle_reasons (fractions in [0, 1]; the throttle bitmask as an integer-valued Float64, see throttle_reasons; NaN when the device does not expose a counter) and, on NVIDIA GPUs with GPM (Hopper and newer) when counters = :auto, sm_util, sm_occupancy (ACHIEVED), fp64_util, dram_bw_util, fp32_util, fp16_util, tensor_util, int_util, pcie_tx_MiBps, pcie_rx_MiBps, nvlink_rx_MiBps, nvlink_tx_MiBps. Interval metrics (GPM) cover the time since the previous gpu_sample of that device (the first call takes a 100 ms interval). This is exactly what the sampler child calls per tick; for a time series while a kernel runs use with_gpu_sampler — an in-process loop is suspended by Julia's GC/timer coupling while the host allocates.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_sample"},{"category":"method","location":"api/#GPUDiagnostics.gpu_sampler_sources-Tuple{KernelAbstractions.Backend, AbstractVector{<:Integer}, Symbol}","page":"API reference","text":"gpu_sampler_sources(backend, device_ids, counters) -> (specs, packages)\n\nVendor hook (implemented by the CUDA.jl / AMDGPU.jl extensions): resolve the (1-based) device_ids into source specs a child process can open WITHOUT the vendor runtime — NVML uuids on NVIDIA (nvml:GPU-<uuid>=<device>; the child loads the listed packages, i.e. CUDA.jl, for the NVML bindings only), amdgpu sysfs paths on AMD (sysfs:…, no packages). counters is :auto or :none. The vendor runtime is touched only here, in the parent.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_sampler_sources"},{"category":"function","location":"api/#GPUDiagnostics.gpu_sm_count","page":"API reference","text":"gpu_sm_count(backend) -> Int\n\nStreaming-multiprocessor (CU on AMD) count of the current device.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_sm_count"},{"category":"method","location":"api/#GPUDiagnostics.gpu_telemetry_stats-Tuple{GPUTelemetry}","page":"API reference","text":"gpu_telemetry_stats(telem; busy_column = :compute_util, busy_threshold = 0.5) -> Dict{String, Any}\n\nReduce a GPUTelemetry over all devices' rows, skipping NaN entries per column: for every metric column <col>_mean, <col>_peak, <col>_busy_mean and <col>_busy_median — over the rows whose busy_column is ≥ busy_threshold, i.e. the kernel-active part of the window without the idle JIT / upload / drain phases diluting it (the number to hold against a kernel's theoretical occupancy) — plus samples (ticks), busy_samples (rows) and, when both power_W and power_limit_W are present, power_capped_fraction: the share of busy rows at power_W ≥ 0.95 × power_limit_W, i.e. running into the power cap. Empty when there are no rows.\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_telemetry_stats"},{"category":"function","location":"api/#GPUDiagnostics.gpu_utilization","page":"API reference","text":"gpu_utilization(backend) -> @NamedTuple{compute, memory}\n\nCurrent-device utilization, each a 0–1 fraction (fraction of recent time the engine was busy).\n\n\n\n\n\n","title":"GPUDiagnostics.gpu_utilization"},{"category":"function","location":"api/#GPUDiagnostics.host_snapshot","page":"API reference","text":"host_snapshot(backend = nothing) -> HostSnapshot\n\nSnapshot of the host and software the run happens on (see HostSnapshot); the vendor version strings are filled in when a backend is given and its extension implements backend_versions. Warnings are returned as data, never logged.\n\n\n\n\n\n","title":"GPUDiagnostics.host_snapshot"},{"category":"method","location":"api/#GPUDiagnostics.hw_counter_command-Tuple{KernelAbstractions.Backend, Cmd}","page":"API reference","text":"hw_counter_command(backend, cmd; set = :issue, dir, name,\n                   metrics = nothing, timeout_s = 600, kill_after_s = 20, ...)\nhw_counter_command(collector::HWCounterCollector, cmd; kwargs...)\n\nBuild a Cmd wrapping a workload in rocprofv3 or Nsight Compute. Nothing is run or written by this function. The caller creates dir and runs the command. Environment and working directory are preserved. metrics overrides the named preset. executable overrides the tool location. A finite timeout uses the external timeout utility; nothing disables it. The backend extension selects the collector. For an external workload, explicitly pass RocprofV3() or NsightCompute() without loading AMDGPU or CUDA. Vendor symbols are used only for offline parsing and preset lookup, not collector selection.\n\nAMD: kernel_trace=true, optional kernel regex filter. NVIDIA: kernel, launch_skip=0, launch_count=nothing, clock_control=:none, cache_control=:all, replay_mode=:kernel. NVIDIA writes <name>_ncu.csv and <name>.ncu-rep; AMD writes <name>_counter_collection.csv and its companion files. Preset metric availability and pass counts depend on architecture. Profiling can replay/serialize work and change cache state; profiled times are not ordinary execution times. Filter and warm up a small workload explicitly.\n\n\n\n\n\n","title":"GPUDiagnostics.hw_counter_command"},{"category":"method","location":"api/#GPUDiagnostics.hw_counter_derived-Tuple{HWCounters}","page":"API reference","text":"hw_counter_derived(hc) -> Dict{String, Vector{Union{Missing,Float64}}}\n\nDerived columns aligned with hc.dispatches, never medians. Unavailable inputs, unsupported architecture-specific normalization, and zero denominators produce missing. Keys whose inputs were not collected are omitted. Common names are used only for defined comparable quantities; vendor-specific estimates keep a vendor prefix. See the hardware-counter guide for denominators and units. Summary reduction is explicit in hw_counter_summary.\n\n\n\n\n\n","title":"GPUDiagnostics.hw_counter_derived"},{"category":"method","location":"api/#GPUDiagnostics.hw_counter_export_command-Tuple{AbstractString}","page":"API reference","text":"hw_counter_export_command(report; output, executable = \"ncu\") -> Cmd\n\nExplicitly export a .ncu-rep to raw CSV with the system tool. This returns a command; hw_counters itself never invokes external software. Output is overwritten by ncu.\n\n\n\n\n\n","title":"GPUDiagnostics.hw_counter_export_command"},{"category":"method","location":"api/#GPUDiagnostics.hw_counter_status-Tuple{KernelAbstractions.Backend}","page":"API reference","text":"hw_counter_status(backend; executable = nothing) -> HWCounterAvailability\nhw_counter_status(collector::HWCounterCollector; executable = nothing)\n\nDiscover the profiler executable without initializing a GPU or asserting profiling permissions. An unsupported backend reports unavailable. A supplied path can locate a tool outside PATH. This does not run the tool.\n\n\n\n\n\n","title":"GPUDiagnostics.hw_counter_status"},{"category":"method","location":"api/#GPUDiagnostics.hw_counter_summary-Tuple{HWCounters}","page":"API reference","text":"hw_counter_summary(hc) -> Dict{String,Any}\n\nFlat reduction with vendor/tool identity, metadata common to every selected dispatch, and <metric>_median/min/max/samples/rel_spread. Native counters have raw_ prefixes and unit keys; derived columns have derived_ prefixes. A total profiled duration is emitted only when every dispatch has a duration. Different device/launch properties are not silently represented by the first dispatch. Missing keys are omitted.\n\n\n\n\n\n","title":"GPUDiagnostics.hw_counter_summary"},{"category":"method","location":"api/#GPUDiagnostics.hw_counters-Tuple{AbstractString}","page":"API reference","text":"hw_counters(path; vendor = nothing, name = nothing, kernel = nothing, slots = nothing,\n            device_overrides = Dict(), provenance = Dict()) -> HWCounters\nhw_counters(vendor::Symbol, path; kwargs...)\n\nParse raw CSV, without GPU packages or system tools. path is an AMD collection directory or counter CSV, or an NVIDIA raw CSV/directory. Detection must resolve exactly one collection; use vendor and/or name to resolve ambiguity. kernel is a substring or regex selecting ONE full kernel name. Without it, exactly one user kernel must be present.\n\nslots is a positive scalar for all selected dispatches, or a vector aligned with them. device_overrides maps native device IDs to dictionaries of known properties, e.g. Dict(1 => Dict(\"n_xcd\" => 8, \"wave_size\" => 64)). Unknown properties stay missing. required_metrics optionally requires finite values of each listed counter on every selected dispatch; use the preset's .metrics to detect silently omitted tool metrics. provenance can record tool_version, clock_control, cache_control, replay_mode, and counter_set; only allowlisted measurement fields are retained, never commands or paths.\n\n\n\n\n\n","title":"GPUDiagnostics.hw_counters"},{"category":"method","location":"api/#GPUDiagnostics.hw_counters_available-Tuple{Union{HWCounterCollector, KernelAbstractions.Backend}}","page":"API reference","text":"hw_counters_available(backend; executable = nothing) -> Bool\nhw_counters_available(collector::HWCounterCollector; executable = nothing)\n\nWhether the backend has a collector and its tool is discoverable. This is NOT a permission check; hw_counter_status leaves permission unknown and does not run the collector.\n\n\n\n\n\n","title":"GPUDiagnostics.hw_counters_available"},{"category":"method","location":"api/#GPUDiagnostics.instruction_mix-Tuple{AbstractString, Symbol}","page":"API reference","text":"instruction_mix(text, vendor::Symbol) -> NamedTuple\n\nStatic instruction mix of a machine-code listing — the pure part of kernel_instruction_mix: vendor is :amd (LLVM AMDGPU ISA text as code_native prints it) or :nvidia (SASS as nvdisasm --print-code prints it). Fields:\n\ntotal — instructions in the listing (every function, every path, each counted once);\ncounts — a NamedTuple over MIX_CLASSES; fp64 its FP64 sum (all six FP64 classes, packed included);\nopcodes — the raw mnemonic histogram (Dict{String, Int}), for drilling into other;\nunclassified, unclassified_opcodes, coverage — instructions no rule of SASS_RULES / AMD_RULES but the catch-all matched (they count as other), their histogram, and 1 − unclassified / total. Deliberate other rules (fences, cache maintenance, special registers, hints) are classified, not unclassified;\nblocks — basic blocks parsed;\nloops — the natural loops of the control-flow graph, largest first, each with header (label), depth (1 = outermost), blocks, total and counts (everything inside the loop, nested loops included) and exclusive_total / exclusive_counts (the loop's own blocks only). Trivial loops (≤ 2 instructions of pure control flow, e.g. the trap spin after EXIT) are dropped;\nhot_loop — the depth-1 loop with the most instructions (nothing when there is none): for a one-work-item-per-thread kernel with an inner per-slot loop this is that loop, and its counts are the static per-slot instruction floor (nested loops counted once);\nhot_loop_confidence — :high when the LLVM assembly printer's own loop annotations are present and agree with the CFG analysis (AMD), :medium when the CFG has a single dominant outer loop (≥ 2× the next), :low otherwise, :none without loops;\nllvm_loops_agree — true/false when LLVM annotations were present, else missing (the listing carries none, so the check cannot be made).\n\n\n\n\n\n","title":"GPUDiagnostics.instruction_mix"},{"category":"method","location":"api/#GPUDiagnostics.kernel_instruction_mix-Tuple{KernelAbstractions.Backend, CompiledKernel}","page":"API reference","text":"kernel_instruction_mix(backend, ck::CompiledKernel; target = nothing, dump = nothing) -> KernelInstructionMix\n\nStatic instruction mix of a compiled kernel (see compiled_kernels): the vendor disassembly of the kernel — the AMD ISA text of code_native, the SASS of CUDA.jl's bundled nvdisasm on the cubin — counted by MIX_CLASSES, with the loop nest recovered from the control-flow graph and the hot loop's counts. All fields of instruction_mix, plus name, signature, target (the ISA the counted code was compiled for: gfx1100, sm_120a, …), native (whether that is the current device's ISA, i.e. whether the counted code is the code that ran), and registers (the VGPR count read from the AMD listing's metadata, to cross-check the runtime's attribute; missing for SASS, where nvdisasm prints none — kernel_resources has the ptxas figure).\n\ntarget = \"gfx942\" / \"sm_90\" cross-compiles the same kernel — same function, argument types, always_inline and static workgroup size — for another architecture through GPUCompiler and counts THAT code, without the hardware: what the MI300X / H100 stream looks like, whether CDNA3 packed FP64 appears, how many waits the scheduler inserted. AMD targets take the gfx name optionally with HIP feature suffixes (\"gfx942:sramecc+:xnack-\"); GCN/CDNA (gfx9*) compile wave64, RDNA (gfx10+) wave32, like HIP. NVIDIA targets are sm_NN (sm_90, sm_100, sm_120a, …) as ptxas names them; a CUDA context must exist, not a device of that architecture. dump (an IO or a path) receives the disassembly.\n\nir = true (default) adds the ir field: kernel_ir_mix of the same job — the typed LLVM-IR operation counts BEFORE the backend, next to the machine-code counts (an IR fp64_fma of 0 against 219 DFMA per slot is the backend's FMA contraction).\n\nNothing is launched; the compile costs a few seconds. Static counts count code, not execution: read hot_loop for the per-iteration floor of the kernel's main loop and total for the whole binary including its cold exception paths; see fp64_issue_floor for turning the former into time.\n\n\n\n\n\n","title":"GPUDiagnostics.kernel_instruction_mix"},{"category":"method","location":"api/#GPUDiagnostics.kernel_ir_mix-Tuple{KernelAbstractions.Backend, CompiledKernel}","page":"API reference","text":"kernel_ir_mix(backend, ck::CompiledKernel; target = nothing) -> NamedTuple\n\nTyped operation count of the OPTIMIZED LLVM IR of a compiled kernel's job (the same job kernel_instruction_mix disassembles; target as there), walked with LLVM.jl by opcode and operand type — the arithmetic before the backend's FMA contraction and div/sqrt expansion. Returns counts (a NamedTuple over IR_CLASSES, all functions of the module), fp64 (its FP64 sum), total, functions (Dict name → counts, for a module that still has out-of-line callees) and target. Whole-module totals: IR loops are not attributed (the machine-code hot loop is the per-slot figure; the IR is for the fusion/expansion ratio).\n\n\n\n\n\n","title":"GPUDiagnostics.kernel_ir_mix"},{"category":"method","location":"api/#GPUDiagnostics.kernel_resources-Tuple{KernelAbstractions.Backend, CompiledKernel}","page":"API reference","text":"kernel_resources(backend, ck::CompiledKernel; block_size = something(ck.workgroup_size, 256))\nkernel_resources(backend, pattern; kwargs...) -> Vector\n\nCompile-time resource report of a compiled kernel (see compiled_kernels), and the theoretical occupancy the vendor runtime computes from it for a launch of block_size threads per block (default: the kernel's static KernelAbstractions workgroup size, else 256). Returns a KernelResources (registers, local/spill and shared memory, const_mem_bytes, max_threads_per_block, a KernelOccupancy block or missing), plus:\n\nisa — a Dict{String, Any} of extra figures read from the compiled code where the vendor exposes them: on AMD, sgpr_count, vgpr_count, agpr_count (CDNA), the sgpr_spill_count/vgpr_spill_count, scratch_bytes, lds_bytes, max_flat_workgroup_size, wavefront_size, code_bytes and the compiler's own occupancy_waves_per_simd estimate (register budget only — compare with occupancy to see whether LDS or the block size is the binding constraint); on NVIDIA the ptx_version and binary_version the kernel was built for, plus the ptxas --verbose figures of a regenerated compile with the kernel's own options: ptxas_registers, stack_frame_bytes (the call-ABI / private-array frame), spill_store_bytes, spill_load_bytes (true register spills — the driver's local-memory figure lumps both together), cumulative_stack_bytes, and ptxas_functions, the device functions assembled out-of-line (a non-empty list beyond the runtime's exception helpers means Julia did not inline them; compare CUDABackend(always_inline = true)). ptxas_matches_attributes says whether that regenerated compile reproduced the runtime's register count.\n\nThe pattern form maps compiled_kernels(backend; pattern) through the report (possibly empty). Nothing is launched; the AMD ISA dump and the NVIDIA PTX regeneration re-run the compiler for a few seconds.\n\n\n\n\n\n","title":"GPUDiagnostics.kernel_resources"},{"category":"method","location":"api/#GPUDiagnostics.launch_lane-Tuple{Nothing, Any}","page":"API reference","text":"launch_lane(timer, backend) -> Int\n\nDevice id the loop's launches are filed under; resolve once per loop, not per launch.\n\n\n\n\n\n","title":"GPUDiagnostics.launch_lane"},{"category":"method","location":"api/#GPUDiagnostics.launch_tick-Tuple{Nothing, Any}","page":"API reference","text":"launch_tick(timer, backend) -> event | nothing\n\nRecord the START event of a launch on the current task's stream (nothing timer ⇒ no-op).\n\n\n\n\n\n","title":"GPUDiagnostics.launch_tick"},{"category":"method","location":"api/#GPUDiagnostics.launch_times-Tuple{LaunchTimer}","page":"API reference","text":"launch_times(timer::LaunchTimer) -> Dict{Int, Vector{Float64}}\n\nPer-device kernel seconds, one entry per launch in launch order. Waits on each launch's stop event, so it is safe to call as soon as the loop has returned.\n\n\n\n\n\n","title":"GPUDiagnostics.launch_times"},{"category":"method","location":"api/#GPUDiagnostics.launch_tock!-Tuple{Nothing, Any, Any, Any}","page":"API reference","text":"launch_tock!(timer, lane, backend, e0)\n\nRecord the STOP event of a launch and file the pair (e0, stop) under device lane (from launch_lane). Call on the launching task, right after the launch (nothing ⇒ no-op).\n\n\n\n\n\n","title":"GPUDiagnostics.launch_tock!"},{"category":"method","location":"api/#GPUDiagnostics.measure_launch_overhead-Tuple{KernelAbstractions.Backend}","page":"API reference","text":"measure_launch_overhead(backend; n = 200, workgroup = 64) -> LaunchOverhead\n\nCost of a kernel launch on backend: n launches of a one-instruction kernel over workgroup work-items, timed three ways (device events per launch, host enqueue time without a sync, host round-trip with a sync per launch), each reported as the median. Sets the floor for how small a work item can be before launches dominate; differs by an order of magnitude between drivers, and between a VM and bare metal. Requires the :events capability. Costs well under a second.\n\n\n\n\n\n","title":"GPUDiagnostics.measure_launch_overhead"},{"category":"method","location":"api/#GPUDiagnostics.measure_peak_flops-Union{Tuple{KernelAbstractions.Backend}, Tuple{T}, Tuple{KernelAbstractions.Backend, Type{T}}} where T<:AbstractFloat","page":"API reference","text":"measure_peak_flops(backend, T = Float64; n_threads = 2^20, workgroup = 256, trials = 5,\n                   target_seconds = 0.2) -> Float64\n\nAttainable vector peak of backend for element type T in FLOP/s, measured with a dependent-FMA-chain kernel (8 independent chains per thread over n_threads threads). The chain length is calibrated so one launch lasts about target_seconds, then the best of trials launches is returned (best-of, like LinearAlgebra.peakflops, discards clock-ramp and scheduling noise). FLOP = threads × chains × iterations × 2 (one FMA = 2 FLOP). Never routed to matrix/tensor units: this is the ceiling for scalar code in T. Costs about (trials + 2) × target_seconds of device time.\n\nRequires the :peak_flops capability, and :fp64 for T = Float64. On the CPU backend the probe measures one scalar chain per work-item, which under-reports the host by its SIMD width; LinearAlgebra.peakflops is the vectorised host number.\n\n\n\n\n\n","title":"GPUDiagnostics.measure_peak_flops"},{"category":"method","location":"api/#GPUDiagnostics.sampler_source-Tuple{AbstractString, Symbol}","page":"API reference","text":"sampler_source(spec::AbstractString, counters::Symbol) -> SamplerSource\n\nBuild the source described by spec (\"<kind>:<payload>\"). Kinds shipped here: sysfs (amdgpu driver files: sysfs:<device>:<power>:<busy>:<membusy>:<vram>[:<sclk>:<mclk>:<temp_edge>:<temp_hot>:<power_cap>], - for a file the device does not have; the five trailing fields may be omitted altogether) and synthetic (synthetic:<device>, deterministic fake values for tests); the CUDA extension adds nvml (nvml:GPU-<uuid>=<device>). counters is :auto (hardware counters when the device has them) or :none. Unknown kinds throw.\n\nEvery source's sample! returns the base columns power_W, compute_util, mem_util, vram_used_B, sm_clock_MHz, mem_clock_MHz, temperature_C, hotspot_C, power_limit_W, throttle_reasons (a bitmask stored as an integer-valued Float64; decode with throttle_reasons), NaN where the device or vendor has none.\n\n\n\n\n\n","title":"GPUDiagnostics.sampler_source"},{"category":"method","location":"api/#GPUDiagnostics.supports-Tuple{KernelAbstractions.Backend, Val}","page":"API reference","text":"supports(backend, feature::Symbol) -> Bool\nsupports(backend, ::Val{feature}) -> Bool\n\nWhether backend implements the subsystem feature (one of FEATURES). The default is false; the KA CPU backend declares :devices, :events, :peak_flops, :fp64 and :kernel_inventory (an empty inventory), and each vendor extension declares what its runtime can answer. A backend extension adds a method per feature:\n\nGPUDiagnostics.supports(::MyBackend, ::Val{:telemetry}) = true\n\nAn entry point of an undeclared feature throws BackendUnsupported.\n\n\n\n\n\n","title":"GPUDiagnostics.supports"},{"category":"method","location":"api/#GPUDiagnostics.telemetry_child_main-Tuple{AbstractVector{<:AbstractString}}","page":"API reference","text":"telemetry_child_main(args) -> Int\n\nEntry point of the sampler child (a separate Julia process started by with_gpu_sampler). args: --dt=<s> --ppid=<pid> --stop=<stopfile> --counters=<auto|none> <spec>.... Opens one sampler_source per spec, prints a header comment naming the columns (# epoch_s  device  <metric…> — the union of the sources' first samples, base columns first), then one TSV row per device every dt seconds on stdout (nan for a missing metric), and returns when the stopfile appears or the parent dies. Never loads a vendor runtime beyond what the parent asked for (packages), so it starts in about a second.\n\n\n\n\n\n","title":"GPUDiagnostics.telemetry_child_main"},{"category":"method","location":"api/#GPUDiagnostics.thread_fill_occupancy-Tuple{KernelAbstractions.Backend, Integer}","page":"API reference","text":"thread_fill_occupancy(backend, n_threads) -> Float64 | missing\n\nThread-fill occupancy: the fraction of the current device's total resident-thread capacity (gpu_sm_count × gpu_max_threads_per_sm) a launch of n_threads can fill (e.g. the pixel count of a pixel-parallel kernel). This is an UPPER BOUND on achieved occupancy — per-thread registers / shared memory cap it further; kernel_resources reports that compile-time bound.\n\n\n\n\n\n","title":"GPUDiagnostics.thread_fill_occupancy"},{"category":"method","location":"api/#GPUDiagnostics.throttle_reasons-Tuple{Real}","page":"API reference","text":"throttle_reasons(x::Real) -> Vector{Symbol}\n\nDecode a throttle_reasons sample (the NVML clocks-event bitmask, stored in the telemetry as an integer-valued Float64) into its set bits: :gpu_idle (0x1), :applications_clocks_setting (0x2), :sw_power_cap (0x4), :hw_slowdown (0x8), :sync_boost (0x10), :sw_thermal_slowdown (0x20), :hw_thermal_slowdown (0x40), :hw_power_brake_slowdown (0x80), :display_clock_setting (0x100), :board_limit (0x200), :reliability (0x400). NaN (no vendor bitmask) and 0 give an empty vector; an unknown bit is reported as :bit_<n>.\n\n\n\n\n\n","title":"GPUDiagnostics.throttle_reasons"},{"category":"method","location":"api/#GPUDiagnostics.with_gpu_sampler-Tuple{Any, Any, Real}","page":"API reference","text":"with_gpu_sampler(f, backend, dt; devices = 1:1, tracefile = nothing, counters = :auto) -> (f(), telem)\n\nRun f() while a child process samples devices (1-based vendor ids) every dt seconds: power / compute / memory utilization / VRAM / SM and memory clocks / edge and hotspot temperature / power limit / throttle bitmask everywhere (NaN where a vendor has none), plus hardware counters where the device has them (counters = :auto; NVIDIA GPM on Hopper and newer — achieved SM occupancy, FP64 / FP32 / tensor pipe and DRAM-bandwidth utilization, PCIe / NVLink traffic; :none skips them). f is first so the do-block form works. Without tracefile, samples go to a temp file that is deleted after parsing. telem is a GPUTelemetry column table; reduce it with gpu_telemetry_stats.\n\nIf no sampler child can be built for backend (no vendor extension, e.g. the CPU backend) or the child fails to start, a warning is logged and f runs without one — telemetry is never allowed to break the caller.\n\n\n\n\n\n","title":"GPUDiagnostics.with_gpu_sampler"},{"category":"section","location":"capabilities/#Capabilities","page":"Capabilities","text":"capabilities(CUDABackend())        # [:devices, :device_props, :events, :telemetry, …]\nsupports(backend, :native_mix)     # Bool; the CPU backend has :devices, :events, :peak_flops, :fp64, :kernel_inventory\n\nA value a backend cannot answer is missing (HIP has no const-size attribute, SASS lists no register count, a runtime without an occupancy calculator has no occupancy); NaN appears only inside the telemetry matrix, where rows must stay numeric; nothing means \"does not exist / not asked for\" (a kernel with no loop, ir = false). Every entry point belongs to one feature of FEATURES; calling one the backend does not declare throws BackendUnsupported, whose message names the package to load when that is the reason. The conformance suite (test/conformance.jl) checks a backend against its declarations and runs on the CPU backend in the regular tests.","title":"Capabilities"},{"category":"section","location":"capabilities/#Conformance-suite","page":"Capabilities","text":"test/conformance.jl checks a backend against its declarations: every declared feature's entry points return the documented shapes, every undeclared one throws BackendUnsupported — never a MethodError, never a vendor-named string. It runs on the CPU backend in the regular tests and, with a compiled kernel, on real hardware through the hand-run test/gpu suite:\n\nGPUDIAGNOSTICS_GPU=cuda julia --project=test/gpu -e 'using Pkg; Pkg.resolve(); Pkg.instantiate(); include(\"test/gpu/runtests.jl\")'\nGPUDIAGNOSTICS_GPU=rocm …","title":"Conformance suite"},{"category":"section","location":"porting/#Porting-a-backend","page":"Porting a backend","text":"An extension declares its features with GPUDiagnostics.supports(::MyBackend, ::Val{:feature}) = true and implements the hooks behind them. :devices / :device_props / :telemetry are the gpu_* generics of the device API plus gpu_sampler_sources (and a sampler_source(::Val{kind}, …) for a new source kind); :events is gpu_event / gpu_elapsed; :kernel_inventory is backend_compiled_kernels (with backend_wrap_kernel if the kernel type is shaped differently); :resources / :occupancy are backend_kernel_attributes / backend_kernel_occupancy (+ optional backend_kernel_isa_info); :native_mix is backend_kernel_machine_code; :ir_mix is backend_kernel_ir_counts, the one hook every GPU backend should have (a GPUCompiler job over LLVM IR). The backend_* names are public API, documented in their docstrings, not exported.","title":"Porting a backend"},{"category":"section","location":"porting/#The-hooks","page":"Porting a backend","text":":hw_counters implements backend_counter_collector and returns the external collector: NsightCompute() for CUDA or RocprofV3() for AMDGPU. Command construction and discovery dispatch on that collector; the extension does not need to duplicate profiler options. Tool discovery is separate from the backend capability and from profiling permission.\n\nThe telemetry hooks gpu_sampler_sources and sampler_source are documented in the API reference.","title":"The hooks"},{"category":"section","location":"porting/#What-Metal-(or-any-backend-without-FP64,-a-public-ISA-or-telemetry)-declares","page":"Porting a backend","text":":devices, :events, :kernel_inventory, :ir_mix, :peak_flops (Float32 only, so not :fp64), a partial :resources (no register count, occupancy = missing). Everything else stays undeclared and the conformance suite checks that it fails cleanly. A backend that supplies only the IR mix gets a coarser but honest instruction picture — backend FMA contraction is invisible at IR level, which the caveats page explains.","title":"What Metal (or any backend without FP64, a public ISA or telemetry) declares"},{"category":"function","location":"porting/#GPUDiagnostics.backend_compiled_kernels","page":"Porting a backend","text":"backend_compiled_kernels(backend) -> Vector{CompiledKernel}\n\nBackend hook (:kernel_inventory): every kernel this process has compiled for backend, from the vendor's kernel-instance cache, each wrapped with backend_wrap_kernel. compiled_kernels filters over it.\n\n\n\n\n\n","title":"GPUDiagnostics.backend_compiled_kernels"},{"category":"function","location":"porting/#GPUDiagnostics.backend_wrap_kernel","page":"Porting a backend","text":"backend_wrap_kernel(kernel) -> CompiledKernel\n\nBuild a CompiledKernel from a vendor kernel object of type K{F, TT} (both CUDA.jl and AMDGPU.jl parametrize their kernel struct by the Julia function type and the argument tuple type). A backend whose kernel type is shaped differently adds its own method.\n\n\n\n\n\n","title":"GPUDiagnostics.backend_wrap_kernel"},{"category":"function","location":"porting/#GPUDiagnostics.backend_kernel_attributes","page":"Porting a backend","text":"backend_kernel_attributes(backend, kernel) -> NamedTuple\n\nBackend hook (:resources): what the compiler gave the vendor kernel object, as (; registers, local_mem_bytes, shared_mem_bytes, const_mem_bytes, max_threads_per_block) — a field the runtime cannot report is missing (a negative count is normalised to missing by kernel_resources).\n\n\n\n\n\n","title":"GPUDiagnostics.backend_kernel_attributes"},{"category":"function","location":"porting/#GPUDiagnostics.backend_kernel_occupancy","page":"Porting a backend","text":"backend_kernel_occupancy(backend, kernel, block_size) -> NamedTuple\n\nBackend hook (:occupancy): the runtime's occupancy calculator for kernel at block_size and the device capacities it is measured against, as (; active_blocks_per_sm, warp_size, max_threads_per_sm, shared_mem_per_sm).\n\n\n\n\n\n","title":"GPUDiagnostics.backend_kernel_occupancy"},{"category":"function","location":"porting/#GPUDiagnostics.backend_kernel_isa_info","page":"Porting a backend","text":"backend_kernel_isa_info(backend, ck::CompiledKernel) -> Dict{String, Any}\n\nBackend hook (optional): vendor extras for the resource report — SGPR/VGPR/spill counts from the AMD ISA dump, ptxas figures on NVIDIA. Default: empty.\n\n\n\n\n\n","title":"GPUDiagnostics.backend_kernel_isa_info"},{"category":"function","location":"porting/#GPUDiagnostics.backend_kernel_machine_code","page":"Porting a backend","text":"backend_kernel_machine_code(backend, ck::CompiledKernel, target) -> NamedTuple\n\nBackend hook (:native_mix): the disassembly of ck for target (nothing = the current device), as (; text, vendor::Symbol, isa::String, native::Bool, registers::Union{Int, Missing}). vendor selects the classifier rules (:amd, :nvidia).\n\n\n\n\n\n","title":"GPUDiagnostics.backend_kernel_machine_code"},{"category":"function","location":"porting/#GPUDiagnostics.backend_kernel_ir_counts","page":"Porting a backend","text":"backend_kernel_ir_counts(backend, ck::CompiledKernel, target) -> (; functions, isa)\n\nBackend hook (:ir_mix, required of every GPU backend): the optimized LLVM IR of ck's GPUCompiler job for target, walked with the LLVM.jl-based _ir_counts helper into functions::Dict{String, IRCounts} plus the isa string the job compiled for.\n\n\n\n\n\n","title":"GPUDiagnostics.backend_kernel_ir_counts"},{"category":"function","location":"porting/#GPUDiagnostics.backend_versions","page":"Porting a backend","text":"backend_versions(backend) -> NamedTuple\n\nBackend hook (optional): the vendor's version strings for host_snapshot — any of driver, runtime, package (the vendor Julia package and its version). Default: empty.\n\n\n\n\n\n","title":"GPUDiagnostics.backend_versions"},{"category":"function","location":"porting/#GPUDiagnostics.backend_counter_collector","page":"Porting a backend","text":"backend_counter_collector(backend) -> HWCounterCollector\n\nBackend hook (:hw_counters): select the external profiler used by collection commands and tool discovery. The CUDA extension returns NsightCompute() and the AMDGPU extension returns RocprofV3(). CSV parsing does not use this hook or load an extension.\n\n\n\n\n\n","title":"GPUDiagnostics.backend_counter_collector"},{"category":"section","location":"report/#Report-layer","page":"Report layer","text":"merge!(manifest[\"gpu\"], diagnostics_dict(kernel_resources(backend, ck); prefix = \"kernel_\"),\n                        diagnostics_dict(m; prefix = \"kernel_mix_\"), diagnostics_dict(m.ir; prefix = \"kernel_ir_\"),\n                        diagnostics_dict(timer; prefix = \"kernel_\"), diagnostics_dict(telem; prefix = \"sampler_\"))\nr                                   # every result type prints as a small table (show)\nusing DataFrames; DataFrame(telem)  # GPUTelemetry is a Tables.jl column table\n\ndiagnostics_dict gives a flat Dict{String, Any} of TOML-safe scalars under stable keys: keys are additive within a schema version, a value the backend could not report is omitted rather than written as a sentinel, and every dict carries gpudiagnostics_schema. The IR counts are a separate dict so the kernel_mix_ and kernel_ir_ families stay apart.\n\nSchema 2 (package 0.3) replaces the AMD-only counter keys with raw_ and derived_ summary keys. Other result layouts are unchanged. See counter migration.","title":"Report layer"},{"category":"section","location":"report/#Related-packages","page":"Report layer","text":"GPUInspector.jl — NVIDIA-only inspection and micro-benchmarks (peak FLOPS, memory bandwidth, host-device / peer-to-peer transfers, stress tests). GPUDiagnostics is vendor-neutral through the KernelAbstractions backend, and adds the compile-time / static-code side (resource report, instruction mix, cross-compilation) plus the rocprofv3 / Nsight Compute collectors.\nNVTX.jl — range and mark annotations for Nsight Systems timelines. Complementary: NVTX labels host regions for a profiler GUI, LaunchTimer measures individual kernels with device events at run time, without a profiler attached.\nLIKWID.jl — hardware performance counters through LIKWID, primarily for the CPU; LIKWID also has NVIDIA (nvmon) and ROCm (rocmon) counter backends. GPUDiagnostics reaches the GPU counters through the vendor tools instead (NVML GPM counters in-process on NVIDIA, rocprofv3 / Nsight Compute for per-dispatch collections).\nCUDA.jl's @profile and AMDGPU.jl's @roc profiling — the vendor packages' own integrated profilers, for per-kernel timelines of a single session.","title":"Related packages"},{"category":"section","location":"telemetry/#Telemetry","page":"Telemetry","text":"gpu_sample(backend, 1)              # NamedTuple: the base columns below (+ GPM counters on NVIDIA)\n\nresult, telem = with_gpu_sampler(backend, 1.0; devices = 1:2, tracefile = \"gputrace.tsv\") do\n    run_the_workload()\nend\ntelem.columns                       # [:t_rel_s, :device, base columns…, (:sm_util, :sm_occupancy, :fp64_util, …)]\ntelem[:compute_util]                # a column; length(telem) rows over all devices\ngpu_telemetry_stats(telem)          # \"<col>_mean\" / \"_peak\" / \"_busy_mean\" / \"_busy_median\" (rows with compute_util ≥ 0.5),\n                                    # \"samples\", \"busy_samples\", \"power_capped_fraction\" (busy rows at power_W ≥ 0.95 × power_limit_W)\nthrottle_reasons(telem[:throttle_reasons][end])   # e.g. [:sw_power_cap, :sw_thermal_slowdown]\n\nBase columns, every source, NaN where the vendor or device has none:\n\ncolumn unit AMD (amdgpu sysfs) NVIDIA (NVML)\npower_W W hwmon power1_average / power1_input nvmlDeviceGetPowerUsage\ncompute_util, mem_util fraction gpu_busy_percent, mem_busy_percent nvmlDeviceGetUtilizationRates\nvram_used_B B mem_info_vram_used nvmlDeviceGetMemoryInfo\nsm_clock_MHz, mem_clock_MHz MHz hwmon freq1_input, freq2_input nvmlDeviceGetClockInfo (SM, MEM)\ntemperature_C °C hwmon temp1_input (edge) nvmlDeviceGetTemperature (GPU)\nhotspot_C °C hwmon temp2_input (junction) NaN (no NVML junction sensor)\npower_limit_W W hwmon power1_cap nvmlDeviceGetEnforcedPowerLimit\nthrottle_reasons bitmask NaN (the gpu_metrics blob is not decoded yet) nvmlDeviceGetCurrentClocksEventReasons\n\nthrottle_reasons(x) decodes the bitmask into NVML's names (:gpu_idle, :sw_power_cap, :hw_slowdown, :sw_thermal_slowdown, :hw_thermal_slowdown, :hw_power_brake_slowdown, …).\n\ngpu_sample is exactly what the sampler child calls per tick. The child is a Julia process (telemetry_child_main, started with the parent's julia binary and load path) rather than a Julia task: an in-process tick either wedges on the vendor runtime behind a backed-up kernel stream, or is suspended with the sleeping task by Julia's GC/timer coupling while the host thread allocates. The vendor extension resolves per-device source specs in the parent (gpu_sampler_sources) so the child needs no vendor runtime: on AMD it reads the amdgpu driver's sysfs files it was handed and starts in about a second; on NVIDIA it loads CUDA.jl for its NVML bindings only (no CUDA context, a few seconds of startup that telem.first_sample_s records and the starvation watchdog discounts). The child declares its own column header, so the parent parses whatever metric set it emits; it appends rows as it samples (the trace survives a crash) and stops cooperatively via a stopfile, or on its own if the parent dies. Any failure to build or start the child logs a warning and the function runs unsampled.\n\nGPM counters (NVIDIA Hopper and newer). With counters = :auto (default), devices that support GPU Performance Monitoring — H100 / H200 / GH200 / B200 and, with recent drivers, consumer Blackwell (RTX 5090 on driver 580 verified); no profiling privileges needed — add ACHIEVED SM occupancy (the number to hold against the compile-time theoretical occupancy of kernel_resources), FP64 / FP32 / FP16 / tensor / integer pipe utilization, DRAM-bandwidth utilization and PCIe / NVLink traffic, each averaged over the interval between two consecutive ticks. counters = :none skips them. Note that fp64_util is normalised to the SM's full-rate issue slots: a saturated FP64 FMA chain reads ≈ 0.9 on an H100 but only ≈ 1.5 % on a 1/64-rate consumer board.","title":"Telemetry"},{"category":"section","location":"caveats/#Caveats","page":"Caveats","text":"Things that have already cost time, collected so they are read rather than remembered.","title":"Caveats"},{"category":"section","location":"caveats/#Static-counts-are-code,-not-execution","page":"Caveats","text":"kernel_instruction_mix counts every instruction of the binary once, cold exception paths included (bounds checks, DomainError helpers dominate a Julia kernel's code size and never execute). The dynamic figure it stands in for is the hot loop's count: read hot_loop for the per-iteration floor and total for code size. Nested loops are counted once inside their parent, so an inner Newton loop of n iterations adds (n − 1) × its own count per slot on top.","title":"Static counts are code, not execution"},{"category":"section","location":"caveats/#The-instruction-mix-atomic_fallback-class","page":"Caveats","text":"An atomic on a generic (flat) pointer makes LLVM emit private- and shared-address fallback paths (scratch_load/store, ds_*) that a device-memory atomic never executes. They are counted as atomic_fallback, not as memory traffic — read them as \"these atomics take a generic pointer\", not as spills. An address-space-1 pointer removes the paths.","title":"The instruction-mix atomic_fallback class"},{"category":"section","location":"caveats/#IR-mix-versus-machine-mix","page":"Caveats","text":"kernel_ir_mix counts the optimized LLVM IR before the backend: an IR fma count of 0 against hundreds of v_fma_f64 / DFMA is the backend's FMA contraction (Julia lowers muladd to a contract-flagged fmul/fadd pair, counted as fp64_contract); IR div / sqrt against the machine's reciprocal seeds + FMA sequences is its expansion. A backend that offers only the IR mix therefore reports the arithmetic as written, not as issued.","title":"IR mix versus machine mix"},{"category":"section","location":"caveats/#fp64_util-is-normalised-to-full-rate-issue-slots","page":"Caveats","text":"The GPM fp64_util column is the fraction of the SM's full-rate FP64 issue slots: a saturated FP64 FMA chain reads ≈ 0.9 on an H100 but only ≈ 1.5 % on a 1/64-rate consumer board.","title":"fp64_util is normalised to full-rate issue slots"},{"category":"section","location":"caveats/#compute_util-means-different-things-per-vendor","page":"Caveats","text":"NVML's utilization is the fraction of the sample period during which any kernel was resident, not SM busy; the amdgpu driver's gpu_busy_percent is closer to SM busy. Do not average compute_util across vendors; compare within one.","title":"compute_util means different things per vendor"},{"category":"section","location":"caveats/#thread_fill_occupancy-is-an-upper-bound","page":"Caveats","text":"It is the static ratio of a launch's threads to the device's resident-thread capacity. A memory- or power-bound kernel does not get faster by filling more of it (an MI300X split-mode experiment fit two groups at 90 % fill and gained 1.16×, power-pinned at TDP throughout). Hold it against the sampler's achieved occupancy and clocks.","title":"thread_fill_occupancy is an upper bound"},{"category":"section","location":"caveats/#shared_mem_bytes-is-what-the-compiler-reserved","page":"Caveats","text":"LLVM's AMDGPU backend promotes private arrays it cannot keep in registers to LDS, sized for the kernel's maximum block size. kernel_resources reports that reservation — not what the source declared — because the reservation is what caps resident blocks per CU.","title":"shared_mem_bytes is what the compiler reserved"},{"category":"section","location":"caveats/#Nsight-Compute's-\"local-memory-spilling\"-counts-the-call-ABI-too","page":"Caveats","text":"On NVIDIA, out-of-line device functions round-trip sret/byval frames through local memory; ncu labels that as spilling. kernel_resources separates the call-ABI stack frame from true register spills via ptxas --verbose, and CUDABackend(always_inline = true) removes the out-of-line functions altogether (a 2.48× win on one production kernel).","title":"Nsight Compute's \"local memory spilling\" counts the call ABI too"},{"category":"section","location":"caveats/#The-AMD-ISA-dump-regenerates-with-default-compile-options","page":"Caveats","text":"kernel_resources' AMD ISA figures come from a code_native of the kernel's job with default options; a non-default production option is not reflected there.","title":"The AMD ISA dump regenerates with default compile options"},{"category":"section","location":"caveats/#Telemetry-means-over-a-window-that-includes-JIT-and-idle-are-not-the-kernel's-numbers","page":"Caveats","text":"Read the _busy_mean / _busy_median statistics (rows with compute_util ≥ 0.5), and separate the warm phase with first_launch_s / launch_times(timer; skip_first = true). The sampler child takes a few seconds to start on NVIDIA (it loads CUDA.jl for NVML); first_sample_s records it and a window shorter than that has no samples.","title":"Telemetry means over a window that includes JIT and idle are not the kernel's numbers"},{"category":"section","location":"caveats/#The-launch-probe's-device-time-includes-the-event-pair","page":"Caveats","text":"measure_launch_overhead's device_s brackets a one-instruction kernel with two device events, whose own cost is a few microseconds — it is an upper bound and may exceed the host round trip.","title":"The launch probe's device time includes the event pair"},{"category":"section","location":"caveats/#The-peak-probe-reads-low-on-some-architectures","page":"Caveats","text":"The FMA-chain probe reaches ≈ 91 % of the vector FP64 spec on an H100 but ≈ 54 % on an A100 at full boost clock; the shortfall is a probe-geometry question (chains per thread, launch size) tracked in the repository's issues, not a clock effect. Treat the number as \"attainable by scalar code at the clocks the device holds\", and cross-check with the sampler's clock columns.","title":"The peak probe reads low on some architectures"},{"category":"section","location":"caveats/#test/gpu-needs-Pkg.resolve()-first","page":"Caveats","text":"The test/gpu environment dev-tracks the package; a manifest resolved before a dependency was added to Project.toml is not refreshed by instantiate alone.","title":"test/gpu needs Pkg.resolve() first"},{"category":"section","location":"device_timing/#Device-API-and-kernel-timing","page":"Device API and kernel timing","text":"","title":"Device API and kernel timing"},{"category":"section","location":"device_timing/#Device-API","page":"Device API and kernel timing","text":"using GPUDiagnostics, CUDA          # or AMDGPU\nbackend = CUDABackend()\n\ngpu_device_count(backend), gpu_name(backend), gpu_arch(backend)\ngpu_sm_count(backend) * gpu_max_threads_per_sm(backend)   # resident-thread capacity\ngpu_memory_info(backend), gpu_power(backend), gpu_utilization(backend)\nthread_fill_occupancy(backend, n_threads)                 # static upper bound, see Caveats\n\nEverything dispatches on the KernelAbstractions Backend. KernelAbstractions has no device management of its own; gpu_device! is how a multi-device driver pins each shard's task to a GPU (1-based across vendors).\n\nWhen launches are queued asynchronously, a host clock around a launch measures enqueue latency. An event recorded on the launch stream fires when the GPU reaches it in stream order, so a pair around a launch brackets exactly that kernel — two barrier packets per launch, no host stall.\n\ntimer = LaunchTimer()\nlane = launch_lane(timer, backend)          # once per loop (device id)\nfor item in work\n    e0 = launch_tick(timer, backend)\n    my_kernel!(backend)(item; ndrange = n)  # asynchronous\n    launch_tock!(timer, lane, backend, e0)\nend\nlaunch_times(timer)                          # Dict(device => [seconds per launch, …])\n\nPass nothing instead of a timer and the hooks are no-ops. The timer is safe to share across per-device tasks (pushes are locked; events are per-stream).","title":"Device API"},{"category":"section","location":"hw_counters/#Hardware-counters","page":"Hardware counters","text":"Counters answer what the kernel did: instructions issued, waves resident, cache requests, cycles. They do not answer how long it took; that is LaunchTimer's job, with with_gpu_sampler alongside for clocks and power. A performance study therefore runs the same kernel twice, and the split is the same on every vendor:\n\nThe timing run, unprofiled, in the card's normal power state: LaunchTimer medians over warm launches. This is the benchmark.\nThe counter run, a few dispatches of the same kernel under the profiler, built with hw_counter_command. The profiler changes how the kernel runs: Nsight Compute replays and serialises launches, RDNA GPUs need a fixed performance level before some counters read at all, and only CDNA parts profiled within a few percent of their event timings.\n\nTwo kinds of values come out of the counter run. Counts — instructions per slot, waves, requests, hit rates, wait fractions — are exact whatever the clock did, and are the code's behaviour as run. Anything divided by a duration — the active clock, achieved bandwidth, the profiled duration itself — describes the clock state during the collection, which on an RDNA card pinned to its standard level or under ncu's replay is not the state of the timing run. The join between the two runs is the cycle count: every preset carries GRBM_GUI_ACTIVE (or gpu__time_duration with the SM clock) because cycles per slot is the cost of the code independent of clock, and the timing run's duration turns it into the clock the kernel really ran at. That is also how a power-managed part is read: the same work in fewer cycles is a better kernel, wall time falling less than the cycles is the clock dropping.\n\nPer-dispatch counters come from rocprofv3 on AMD and Nsight Compute (ncu) on NVIDIA. Both produce HWCounters. Sampled counters, such as NVML GPM, remain in GPUTelemetry; they measure a different window and have a separate capability.\n\nusing GPUDiagnostics, CUDA, KernelAbstractions\n\nbackend = CUDABackend()              # use ROCBackend() with AMDGPU for AMD\nmkpath(\"prof\")\ncmd = hw_counter_command(backend, `julia --project run.jl`;\n    set = :fp64, dir = \"prof\", name = \"cell\",\n    kernel = \"my_kernel\", launch_skip = 1, launch_count = 2)\nrun(cmd)\nhc = hw_counters(\"prof\"; name = \"cell\", kernel = \"my_kernel\",\n    slots = n_work_items * n_iterations_per_item,\n    required_metrics = COUNTER_SETS[:nvidia][:fp64].metrics,\n    provenance = Dict(\"counter_set\" => \"fp64\", \"clock_control\" => \"none\",\n        \"cache_control\" => \"all\", \"replay_mode\" => \"kernel\"))\nhc.dispatches                       # identity, device, resources and duration of each launch\nhw_counter_derived(hc)               # vectors aligned with those dispatches\nhw_counter_summary(hc)               # medians, ranges and valid sample counts\ndiagnostics_dict(hc; prefix = \"hw_\") # flat, schema-tagged, TOML-safe summary\n\nThe backend's extension selects the profiler: CUDABackend() uses Nsight Compute and ROCBackend() uses rocprofv3. The same intent names apply to both. Pass the backend to hw_counter_status and hw_counters_available as well. Loading both extensions does not introduce a global default; the backend argument determines the collector. NVIDIA launch-skip/count are collector options; omit them for AMD and bound its workloads in the script itself. Other collector-specific keywords belong only to that collector. hw_counter_command executes nothing and creates no directories. It preserves the workload's environment and working directory; relative output paths resolve there.\n\nWhen preparing an external workload without loading a GPU runtime, select the collector explicitly with hw_counter_command(NsightCompute(), cmd; ...) or hw_counter_command(RocprofV3(), cmd; ...). These collector types also work with tool discovery. Vendor symbols remain available for preset lookup and optional CSV parser selection; collection commands use backend or collector dispatch.","title":"Hardware counters"},{"category":"section","location":"hw_counters/#Collection-requirements","page":"Hardware counters","text":"The following setup guidance targets Linux collection. Parsing existing CSV files requires neither a GPU nor a profiler installation. Install the collector on the machine that runs the workload, and use a profiler release that supports its GPU, driver, and operating system. Installing CUDA.jl or AMDGPU.jl alone does not establish that the collector is installed or that counter access is enabled.\n\nRequirement NVIDIA AMD\nCollector Nsight Compute CLI, ncu ROCprofiler-SDK CLI, rocprofv3\nRuntime Working CUDA workload and a compatible NVIDIA driver Working HIP/HSA workload with profiler registration enabled\nAccess GPU access and driver permission to profile counters ROCm device access and, when needed, permission to configure the performance state\nMetrics Full metric names supported by this GPU and ncu release Counters supported by this architecture and rocprofv3 release\nOutput Writable collection directory; CSV or explicit .ncu-rep export Writable collection directory; CSV output enabled\n\nPut the collector on PATH, or pass executable = \"/path/to/collector\" to both hw_counter_status and hw_counter_command. The command builder also uses timeout by default; set timeout_s = nothing if that utility is unavailable and bound the workload yourself.\n\nsupports(backend, :hw_counters) means an implementation exists. hw_counters_available checks tool discovery only. hw_counter_status exposes the executable and leaves permitted = missing: PATH discovery cannot establish profiling permissions. Collector failures remain failures of run(cmd), including access denied. Some tool versions silently omit unknown metrics: required_metrics requires finite values for each listed counter on every selected dispatch and detects such incomplete collections. Neither tool is a dependency of the package or its CSV parsers.","title":"Collection requirements"},{"category":"section","location":"hw_counters/#NVIDIA:-installation-and-profiling-permission","page":"Hardware counters","text":"Install Nsight Compute and check its release-specific GPU/platform support and driver requirements. A working CUDA kernel or nvidia-smi is insufficient to establish counter access.\n\nncu --version\nncu --query-metrics --query-metrics-mode all\n\nThe second command lists full metric names, including suffixes such as .sum, for the devices visible to ncu. Use names from that list; enumeration does not prove collection permission. See the ncu CLI reference.\n\nERR_NVGPUCTRPERM means the driver denied profiling access. An administrator can grant non-admin profiling access or arrange an appropriately privileged profiling process. The mechanism depends on the driver release:\n\nLegacy Linux configuration uses NVreg_RestrictProfilingToAdminUsers. Setting it to 0 permits non-admin profiling; applying the change can require driver reload or reboot.\nR610+ drivers also support access grants through NVIDIA profiling capability device nodes. Follow the driver-specific instructions for user/group access.\n\nFor a read-only check of the legacy setting:\n\ngrep RmProfilingAdminOnly /proc/driver/nvidia/params\n\nOn R610+, this flag alone does not account for capability-based grants. Containers also need the host's profiling access and appropriate device/permission configuration; installing ncu in a container is insufficient. Follow NVIDIA's counter-permission guide. GPUDiagnostics does not change driver permissions or elevate the workload.","title":"NVIDIA: installation and profiling permission"},{"category":"section","location":"hw_counters/#AMD:-installation,-runtime-and-performance-state","page":"Hardware counters","text":"Install ROCprofiler-SDK for the ROCm release used by the workload. Package names vary by release and distribution; for example, AMD's ROCm 7.1/7.2 repositories provide rocprofiler-sdk. Follow the installation instructions for that release and configure the user's ROCm device access before collecting.\n\nrocprofv3 --version\nrocprofv3 --list-avail\n\nThe counter list is architecture-specific. Raw counter support does not imply that an Instinct preset or its derived formulas apply to an RDNA GPU.\n\nOn RDNA3/RDNA4, AMD requires the STABLE_STD performance level (profile_standard in sysfs) for counters in some hardware blocks. In AUTO, wave counts can look valid while GRBM cycle counters remain zero. Select the target GPU explicitly, record its original performance level, and restore it after profiling. The package does not change power settings. See AMD's PMC profiling prerequisites.\n\nFor example, with an amd-smi release supporting these options, replace <gpu-index> with the intended amd-smi GPU index:\n\namd-smi list\nsudo amd-smi metric --gpu <gpu-index> --perf-level\nsudo amd-smi set --gpu <gpu-index> --perf-level STABLE_STD\n\nAfter collection, restore the recorded level (AUTO only if it was originally AUTO):\n\nsudo amd-smi set --gpu <gpu-index> --perf-level AUTO\n\nRestoring is not housekeeping. STABLE_STD fixes the shader clock at a low \"standard\" level and disables idle down-clocking: on a W7900 it held 959 MHz against a 2.9 GHz boost, so every other workload on that GPU ran about three times slower, and idle board power stayed at 94 W instead of 19 W. It also means profiled durations under STABLE_STD are not comparable with unprofiled runs of the same kernel; compare cycle counters, or time the kernel again in AUTO. When scripting, restore the original state on errors and interrupts as well. GPU indices can differ between amd-smi, rocprofv3, and DRM sysfs; identify the same device in each tool. The sysfs knob is unambiguous when the PCI address is known (card<N>/device/uevent lists it):\n\ncat  /sys/class/drm/card<N>/device/power_dpm_force_performance_level          # record: auto\necho profile_standard | sudo tee /sys/class/drm/card<N>/device/power_dpm_force_performance_level\n# ... collect ...\necho auto | sudo tee /sys/class/drm/card<N>/device/power_dpm_force_performance_level\n\nThe HIP/HSA runtime must also support ROCprofiler registration. If the workload runs but no dispatch records appear, verify that the profiler and workload load compatible, profiling-enabled runtime libraries. When using an isolated ROCm installation with AMDGPU.jl, set ROCM_PATH as well as the dynamic library search path for that process.\n\nWith rocprofv3 1.0.0, an unsupported GPU present alongside the target GPU can cause an unordered_map::at exception during counter configuration. Device-qualified metric names, such as metrics = [\"SQ_WAVES:device=0\", \"GRBM_GUI_ACTIVE:device=0\"], restrict collection to the selected profiler GPU index. The CSV retains the unqualified names; use those for required_metrics. Enumerating counters or checking that values are finite does not establish their correctness: validate them against a workload with known counts.","title":"AMD: installation, runtime and performance state"},{"category":"section","location":"hw_counters/#Verify-a-small-collection-first","page":"Hardware counters","text":"Run a bounded workload that passes a numerical correctness check, then collect only a few advertised metrics. For AMD, SQ_WAVES and GRBM_GUI_ACTIVE provide a useful initial check when supported; add :device=N to each collection metric when needed. For NVIDIA, start with gpu__time_duration.sum and one advertised instruction counter. Validate the dispatch count, units, and expected activity before collecting a full preset.\n\nThe repository's test/gpu/counter_probe.jl is a small KernelAbstractions FMA workload for either vendor, selected with GPUDIAGNOSTICS_GPU=cuda or rocm. Its three launches each process 4096 work-items. When checking AMD wave counts, use the kernel's actual wave width: 4096 work-items correspond to 128 waves at width 32, or 64 at width 64.\n\nSymptom First check\nCollector not found Installation, PATH, or explicit executable\nNVIDIA ERR_NVGPUCTRPERM Driver profiling permissions for the collecting process\nAMD workload runs but no dispatch CSV appears HIP/HSA profiler registration, loaded runtime paths, and kernel filter\nAMD wave counts are plausible but GRBM counts are zero RDNA performance state before collection\nrocprofv3 1.0.0 throws unordered_map::at with mixed GPUs Device-qualified metrics for the supported target\nMetrics absent from otherwise successful output Target-specific names; pass unqualified CSV names to required_metrics","title":"Verify a small collection first"},{"category":"section","location":"hw_counters/#Presets-and-collection-behavior","page":"Hardware counters","text":"Intent AMD (rocprofv3) answers NVIDIA (ncu) answers\n:issue per-wave issue by class → amd_insts_per_slot_<class>, the dynamic mix to hold against the static kernel_instruction_mix hot loop warp and thread instructions → nvidia_insts_per_slot; thread / (32 × warp) below 1 is divergence\n:occupancy resident waves and wait fractions (quad-cycle SQ counters) → latency-bound vs issue-bound achieved warps per SM over active cycles → nvidia_active_occupancy\n:memory TA / TD busy, TCP stalls, L1 miss → is the in-order vector-memory pipe the wall DRAM bytes → achieved bandwidth\n:fp64 FMA / ADD / MUL / TRANS F64 per wave → fp64_flop_per_slot against the algorithmic count DFMA / DADD / DMUL per thread → fp64_flop_per_slot\n:l2 TCC hits / misses, fabric and HBM reads → does the working set live in L2 or HBM L2 sector hit rate → nvidia_l2_sector_hit_rate\n\nEach CounterSet.notes carries the long form: what the counters are (per-wave, per-thread, event counts, quad-cycles), the derived keys, the validation and the trap specific to that preset. Print COUNTER_SETS[:amd][:memory].notes before interpreting a collection.\n\nCOUNTER_SETS[vendor][intent] is a CounterSet with native metrics, validation context, known pass count (or missing), and notes. Intents are :issue, :occupancy, :memory, :fp64, and :l2. Use metrics = [\"native_metric\", ...] for a custom collection. AMD presets fit one pass on gfx942 with rocprofv3 1.1.0 / ROCm 7.2.4; parser and derived quantity tests use saved MI300X captures. Passing a GPU execution test does not validate hardware-counter collection. Capacity and metric support on other architectures require validation. NVIDIA metric support and replay cost also vary by GPU and profiler version. On a bounded sm_120 FMA probe with ncu 2025.4.1, issue/occupancy/memory used one pass and FP64/L2 used three. These are validation observations, not portable guarantees; query the target tool first.\n\nCommands use timeout -k to bound the profiler and workload if collection hangs. Set timeout_s = nothing when managing process lifetime yourself, including on systems without that utility.\n\nNVIDIA defaults are clock_control = :none, cache_control = :all, and replay_mode = :kernel. Profiling can replay kernels, flush caches and serialize work; its duration is not an ordinary concurrent launch duration. For application-managed cache priming, consider replay_mode = :application, cache_control = :none. This reruns the application, so ensure repeating its side effects is acceptable. Keep workloads small and record the settings. See the NVIDIA profiling guide.","title":"Presets and collection behavior"},{"category":"section","location":"hw_counters/#Reading-the-counters","page":"Hardware counters","text":"The raw values are the profiler's, in the profiler's units. These are the readings that have already cost time (rocprofv3 1.1.0 on gfx942 unless stated):\n\nEvery AMD counter is summed over its hardware dimensions. GRBM_GUI_ACTIVE has DIMENSION_XCC[0:7] on the MI300X, so a 40 ms dispatch reports 5.7e8 \"cycles\", a 14 GHz clock, until divided by the eight dies. The dispatch's cycle count is GRBM_GUI_ACTIVE / n_xcd (Num_Xcc in the agent info; 1 on single-die parts) and every per-cycle rate uses it. 14 GHz means the division was forgotten; \"/38\" and \"/(8 × 304)\" are the same per-CU normalisation.\n*_sum unit counters are event counts summed over instances (one TA / TD / TCP per CU): a unit's busy fraction is X_BUSY_sum / (cycles × n_cu). Never multiply them by the wavefront size — doing so once inflated per-slot L1 accesses 64×. Only SQ_INSTS_* are per-wave issues, and only those take × wave_size / slots.\nSQ wave-cycle counters are quad-cycles on CDNA and plain cycles on RDNA (SQ_WAVE_CYCLES, SQ_WAIT_ANY, SQ_WAIT_INST_ANY, SQ_ACTIVE_INST_*). AMD's own OccupancyPercent is 400 × SQ_WAVE_CYCLES / max_xcc(GRBM_GUI_ACTIVE) / CU_NUM / 32 on gfx90a, gfx940–942 and gfx950 and 100 × … on gfx10, gfx11 and gfx12 (rocprofiler-sdk counter_defs.yaml). The parser records that unit per dispatch as device[\"sq_cycle_unit\"] (4, 1, or missing for an architecture outside the table; supply it through device_overrides there), and resident waves are sq_cycle_unit × SQ_WAVE_CYCLES / cycles. Ratios between the wave-cycle counters are unit-free. SQ_BUSY_CYCLES is plain cycles everywhere but is summed over a different set of instances: per shader engine on CDNA (DIMENSION_SHADER_ENGINE, 32 on the MI300X) and per WGP on RDNA (DIMENSION_WGP × SHADER_ARRAY × SHADER_ENGINE, 48 on the 96-CU W7900), so amd_sq_busy divides by device[\"sq_instances\"], resolved the same way (n_se on CDNA, n_cu ÷ 2 on RDNA, or an override). Note that AMD normalises by the maximum over dies while the CSV holds the sum, so / n_xcd is the mean over dies: the same while every die is busy, below the maximum otherwise.\namd_active_clock_GHz is the mean active clock, not the engine clock. Idle dies count no cycles, so it is a lower bound and meaningless for sub-ms dispatches. It agreed with the amdgpu sysfs engine clock to 0.01 GHz on the MI300X (1.70 and 1.25 GHz). The MI300X is power-managed: the same kernel ran at 1.70 GHz at 7 waves/CU and 1.25 GHz at 14 waves/CU against the 750 W board cap, so a launch doing 1.8× the work per cycle finished only 1.27× sooner. Compare two runs of the same work in cycles (GRBM_GUI_ACTIVE / n_xcd, SQ_BUSY_CYCLES), not seconds, and report the clock next to the timing.\nCounter capacity is per hardware block and per pass. The TCC block takes four counters on gfx942; a fifth (TCC_REQ_sum, TCC_READ_sum) makes rocprofv3 log Request exceeds the capabilities of the hardware to collect, abort with SIGABRT and leave the profiled child hung. hw_counter_command wraps the collection in timeout -k for exactly that failure (the child then exits 137); grep the profiler log for exceeds the capabilities or caught signal when a collection produced no CSV.\nAMD profiled durations are close to device-event timings (26.8–27.4 ms per dispatch against a 28.0 ms LaunchTimer median): kernels run at speed under --pmc and the overhead is the counter read. NVIDIA durations are replays under clock_control / cache_control and are not benchmarks; instruction counts are exact either way. On the MI300X the FP64 preset reproduced the static hot loop's FMA / ADD / MUL / TRANS counts per slot exactly, bit-identical across dispatches — the check to repeat on a new architecture before trusting a preset.\nNVIDIA thread-instruction metrics are per thread, so / slots directly, no wave factor. nvidia_fp64_pipe_peak_fraction needs sm__pipe_fp64_cycles_active.avg.pct_of_peak_sustained_active, which is not in the :fp64 preset; add it through metrics when the question is \"is the FP64 pipe the wall\" (a kernel at 88 % pipe fraction gained only 7–9 % from removing integer work).\ngfx1100 (Radeon Pro W7900) is validated for the SQ counters, nothing else. It exposes no F64, TA / TD / TCP or TCC counters, so only SQ_* and GRBM_GUI_ACTIVE can be collected and the :memory, :fp64 and :l2 presets do not apply. A W7900 collection (fixture w7900_*) confirmed the RDNA row of the table: SQ_INSTS_VALU × 32 / slots is 1.0001 per FMA, resident waves come out at 1346 of the 3072 the hardware holds (a ×4 unit would exceed it), and SQ_BUSY_CYCLES is 0.89 of the dispatch per WGP (7.1 per shader engine). Three things to know: in AUTO the run \"succeeds\" with SQ_WAVES correct while GRBM_GUI_ACTIVE and SQ_WAVE_CYCLES read zero, and STABLE_STD pins the shader clock at 959 MHz, so profiled durations are three times an unprofiled run; with the host's integrated GPU present, ROCR_VISIBLE_DEVICES does not hide it from rocprofv3 1.1 and unqualified counters abort with unordered_map::at — device-qualified names (SQ_WAVES:device=0) are mandatory; and SQ_WAVES read 14× too high on one dispatch in three of five collections, so validate the wave count per dispatch rather than trusting a median. amd_active_clock_GHz read 1.03 GHz against the 959 MHz sysfs shader clock, consistent with RDNA3's front-end clock running above the shader clock; treat it as the front-end clock there. The container recipe that ran: rocm/dev-ubuntu-24.04 plus libdw1, --device=/dev/kfd --device=/dev/dri --security-opt seccomp=unconfined --ipc=host, the host's juliaup and depot bind-mounted with HOME and JULIA_DEPOT_PATH set, and ROCM_PATH=/opt/rocm.\n\nKernel names as the tools report them: KernelAbstractions kernels are gpu_<kernel name>(…), AcceleratedKernels' foreachindex is gpu__forindices_global_(…), and runtime-internal __amd_rocclr_* dispatches are excluded by the default kernel selection.","title":"Reading the counters"},{"category":"section","location":"hw_counters/#Portable-input-and-retained-data","page":"Hardware counters","text":"hw_counters accepts a directory or CSV and detects the tool from its header. Multiple collections are an error: provide name, vendor, or the CSV path. Kernel selection must resolve to one distinct full name. Each selected dispatch retains its own device, process, context, queue, launch geometry and resources. Tool IDs keep their original indexing; dispatch ID alone need not be globally unique. AMD agent metadata is matched by ID.\n\nNVIDIA wide raw exports (including their units row) and long metric/value exports are supported. Quoted signatures and numeric grouping separators are accepted. Unknown counters are missing, not zero. Conflicting duplicate counters, units or identities are errors. Categorical NVIDIA device/launch attributes stay in dispatch metadata. Native numeric values retain their units; the command builder requests base units.\n\nNative access is hc[\"SQ_WAVES\"] or hc[\"smsp__inst_executed.sum\"]. hc.units records ncu's units; AMD CSV does not supply units, so those entries are missing. slots can be one positive count or a vector aligned with the selected launches. Supply missing known device properties explicitly:\n\nhc = hw_counters(:amd, \"prof\"; name = \"cell\", kernel = \"my_kernel\",\n    device_overrides = Dict(1 => Dict(\"architecture\" => \"gfx942\", \"n_xcd\" => 8,\n        \"n_cu\" => 304, \"wave_size\" => 64, \"max_waves_per_cu\" => 32)))\n\nCycle normalisation applies wherever n_xcd is known (agent info or override); the only architecture-specific input is how the SQ block reports, taken from AMD's own definitions per family or from device_overrides: \"sq_cycle_unit\" (4 on CDNA, 1 on RDNA) and \"sq_instances\" (shader engines on CDNA, WGPs on RDNA). Fixture-validated on gfx942; the RDNA entries follow AMD's counter_defs.yaml and a W7900 collection. Use actual known device properties for overrides.\n\nBinary .ncu-rep input requires an explicit export using the system tool:\n\nrun(hw_counter_export_command(\"cell.ncu-rep\"; output = \"cell_ncu.csv\"))\nhc = hw_counters(\"cell_ncu.csv\")\n\nProvenance accepts only tool_version, clock_control, cache_control, replay_mode, counter_set, and passes. Supply known settings; CSV need not contain them. Parsers discard host names, executable names and tool log messages. Raw reports can contain machine information; sanitize them before publishing fixtures.","title":"Portable input and retained data"},{"category":"section","location":"hw_counters/#Derived-quantities","page":"Hardware counters","text":"hw_counter_derived returns columns, not medians. Missing inputs and zero denominators give missing; absent metric families are omitted. Summaries skip missing samples and report the number of contributing observations.\n\nKey Meaning\ninsts_per_slot_fp64_fma/add/mul NVIDIA predicated-on thread instructions / slots; AMD wave instructions × wave size / slots (assumes full active waves; divergence can overestimate active-lane work).\nfp64_flop_per_slot Twice FMA plus add and multiply counts; excludes transcendental seeds and matrix work.\namd_active_clock_GHz GRBM_GUI_ACTIVE / n_xcd / duration_s / 1e9; equals engine clock only while every die stays busy.\namd_elapsed_occupancy sq_cycle_unit × SQ_WAVE_CYCLES / (GRBM_GUI_ACTIVE / n_xcd) / n_cu / max_waves_per_cu; the unit is 4 on CDNA, 1 on RDNA.\nnvidia_active_occupancy SM active-warp percentage of sustained peak over active cycles / 100. Its averaging window differs from AMD's estimate.\namd_l2_request_hit_rate TCC hits / (hits + misses).\nnvidia_l2_sector_hit_rate L2 sector lookup hit percentage / 100; sectors and requests are distinct denominators.\nnvidia_fp64_pipe_peak_fraction FP64 pipe percentage of sustained peak over active cycles / 100; not a portable fraction of wall time issuing FP64.\n\nAMD issue, wait, residency and unit-busy estimates keep amd_ names. The formulas retain the SQ wave-cycle unit and the die / engine dimensions AMD's own derived metrics use. Native counts remain available to audit them. Profiler local-memory traffic can include call-ABI stack frames as well as actual register spills.","title":"Derived quantities"},{"category":"section","location":"hw_counters/#Migration-from-0.2","page":"Hardware counters","text":"Version 0.3 removes the AMD-only public family:\n\nOld Replacement\nrocprof_command(cmd; counters = :sq_issue, ...) hw_counter_command(ROCBackend(), cmd; set = :issue, ...), or RocprofV3() without AMDGPU\nROCPROF_COUNTER_SETS COUNTER_SETS[:amd] (:sq_waves → :occupancy, :l1_pipe → :memory)\nrocprof_available() hw_counters_available(ROCBackend()), or hw_counters_available(RocprofV3()) without AMDGPU\nrocprof_counters(dir; ...) hw_counters(:amd, dir; ...)\nRocprofCounters HWCounters, with metadata per HWDispatch\nrocprof_derived(rc) hw_counter_derived(hc) returns vectors; summarize explicitly\nrocprof_median(rc, counter) Statistics.median(skipmissing(hc[counter])) when samples exist\nrocprof_summary(rc) hw_counter_summary(hc)\nfp64_flop_per_slot = 2·FMA + ADD + MUL + TRANS fp64_flop_per_slot = 2·FMA + ADD + MUL on both vendors; the AMD TRANS count stays in amd_insts_per_slot_valu_trans_f64\n\nSchema 2 replaces counter summary keys with raw_<name>_median and derived_<name>_median, plus ranges and valid sample counts. Other result layouts are unchanged. Changing a prefix alone does not migrate old reports. Unknown device properties are missing; summaries include only properties common to all selected dispatches. The result holds selected counter dispatches; an AMD kernel-trace companion is no longer folded into the counter result.","title":"Migration from 0.2"},{"category":"section","location":"#GPUDiagnostics.jl","page":"Home","text":"Vendor-neutral runtime diagnostics for KernelAbstractions code. Everything dispatches on the KA Backend; CUDA.jl and AMDGPU.jl package extensions supply the vendor methods, and the CPU backend gets host fallbacks so the plumbing runs (and is tested) without a GPU. No kernel is modified by any of it.\n\nNot registered yet: pkg> add https://github.com/SebastianM-C/GPUDiagnostics.jl.\n\nusing GPUDiagnostics, CUDA          # or AMDGPU\nbackend = CUDABackend()\n\ngpu_device_count(backend), gpu_name(backend), gpu_arch(backend)\ngpu_sm_count(backend) * gpu_max_threads_per_sm(backend)   # resident-thread capacity\ngpu_memory_info(backend), gpu_power(backend), gpu_utilization(backend)","title":"GPUDiagnostics.jl"},{"category":"section","location":"#What-is-where","page":"Home","text":"Capabilities — which subsystems a backend implements, BackendUnsupported, the conformance suite.\nDevice API and kernel timing — device enumeration and properties, device-event LaunchTimer.\nTelemetry — the out-of-process sampler, its columns, GPM counters, stats.\nMeasured peak and cheap probes — FMA-chain peak FLOP/s, launch overhead, host snapshot, warm-up accounting.\nResource report and instruction mix — registers / spills / occupancy, the static ISA and IR mix, cross-compilation, the FP64 issue floor.\nHardware counters — rocprofv3 / Nsight Compute collectors and a shared per-dispatch result.\nReport layer — diagnostics_dict, show, Tables.jl.\nPorting a backend — the backend_* contract.\nCaveats — the traps that have already cost time.\nAPI reference","title":"What is where"}]
}
